# 更新日志（CHANGELOG）

AgentCore 是多轨道 monorepo，各客户端 / 服务**独立 semver**（见 [`docs/05-平台与运维/发布与门禁.md` §7.1 版本钉定](docs/05-平台与运维/发布与门禁.md)）。本文件按「**轨道 × 版本**」组织：每条轨道一节，版本自上而下降序；每次发布前，为本次 bump 的轨道补写本版说明。

- **写法**：面向用户、中文、分点，归纳自「本轨道自上一个发布 tag 以来的 git log」。
- **投递**：桌面版说明进 GitHub release 正文；其余轨道随各自发布渠道发出。
- **轨道**：桌面客户端（`desktop`）· 后端 API（`api`）· 手机端（`mobile`）· 管理后台（`admin`）· 官网（`website`）。

> 发布流程与本文件在 checklist 中的位置：见 [`docs/05-平台与运维/发布与门禁.md` §7.8 上线检查清单结构](docs/05-平台与运维/发布与门禁.md)。

---

## 桌面客户端（desktop）

### v0.9.12 — 2026-08-25

- 组队 / 开辩不再先挂开工确认卡：编制到即开跑。历史上未处理的开工卡不再出现继续 / 取消，续跑会诚实失败。
- 助手回复支持工作区图片对比围栏：聊天过程正文与文件预览都能从工作区加载图片；外链不会自动拉取。
- 工具失败改成产品可读说明；协作图可停整轮或停单个成员。
- AI 选错工具通道时（例如用跑代码去搜源码），过程里显示「改用搜索」一类步骤，不再标成失败。
- 新开辩论在图上标「自动开辩」；旧图上的授权标记改称「已授权开跑」，不再写成开工卡。

### v0.9.4 — 2026-08-17

- 长回合断线后持续重连：间隔从约 1 秒指数退避到 30 秒封顶并带抖动；切走对话、你主动停止、或服务端已结算本回合后才停。
- 排查包脱敏：只收录白名单事件与字段，对话正文与 token 不进包。
- 重连横幅文案统一；断线恢复与冷启动重连路径更稳。
- 同一对话只保留最新一张开工卡，与后端投影对齐，避免叠多张可点卡片。

### v0.6.36 — 2026-08-05

- 发现新版本后先说明再更新：同意后后台下载，进度在「设置 · 关于」，下完再重启安装。
- 切换 / 重开对话时消息窗写入更稳，减少旧内容覆盖。
- 助手脚注突出费用、轮次、用时；本地工作区列表补齐大小与修改时间。

### v0.6.17 — 2026-07-31

- 本机 MCP：工具箱「连接器」可配置 stdio MCP Server；Agent 调用需你确认。
- 常驻任务模板「每日对话回顾」：引导安装，确认提案后写入偏好 / 档案等。
- 交付清单改为路径级验收：展示 accepted / rejected；「已交付」只计验收通过文件。
- 去掉回合初「开工提案卡」专用壳；开场改为普通短问或按默认直接派工。
- 刷新或恢复会话时，未处理的审批 / 授权 / 升级卡会重新挂出。
- 产品手册与连接器 / 日回顾说明同步。

### v0.6.16 — 2026-07-31

- 消息 IM：官方号产品公告收件箱（设置·公告改跳官方号）；单聊/群在线态绿点；回复引用；@人 / @所有人（菜单、高亮、静音弱提醒）。
- 权限配方：默认「少打断」；托管 / 谨慎档位与深度研究自治指引对齐；对话内权限切换提示延续。
- 跟进推荐：不可用时诚实标记，避免空 chips。
- 错误文案：空失败气泡 / 无产出收束更易读。

### v0.6.15 — 2026-07-30

- 本机 / 白板 / 工作区 Client Tool：断线或恢复会话后可重新挂上待履行操作，减少「等你确认却丢了」的空窗。
- 升级卡与履行链路打磨：本机协助等需你确认的操作在续跑后更稳地送达桌面。
- 安全依赖升级。

### v0.6.14 — 2026-07-30

- 辩论庭前证据包：开赛前可见各方取证进度与证据台账增量，庭前区展示更完整。
- 结辩姿态与台账接线：辩论收束与客户端证据呈现对齐。

### v0.6.13 — 2026-07-29

- 本机 Host 通道：Agent 可经桌面履行本机操作（信息 / 存储 / 网络摘要 / 受控 shell 等），需你确认时走权限第四轴「本机」。
- 权限四轴：会话权限在「改文件 / 执行 / 组团」之外增加「本机」档位；配方与对话内提示对齐。
- 浏览器与工作区硬化：会话隔离、附件与路径边界加固。
- 产品手册与法律文案同步（隐私 / 条款相关呈现）。

### v0.6.12 — 2026-07-28

- 本机浏览器真画面：Agent 打开的网页与右坞「浏览器」同页可见，可接管 / 归还；工作区 HTML「完整预览」走同一壳。
- 站立任务（自动化）：工具箱可建定时 / Webhook 任务，代跑后进个人收件箱审阅；挂起拍板后收件箱可收口。
- 权限三轴：会话权限改为「改文件 / 执行 / 组团」独立档位；对话内可见切换提示。
- 对话冷加载诚实壳：打开历史会话时不再误像空草稿可发送。
- 消息图片画廊：IM / 对话附件图可点开预览。
- 本地跑代码：`code_execute` 自动带上工作区根与 `src`/`lib`，更易跑通常见项目布局。

### v0.6.11 — 2026-07-27

- 维护性发版：对齐后端 api 0.3.16 / 手机 0.3.8；发布链路与依赖小修。

### v0.6.10 — 2026-07-26

- Markdown 导出 Word：文件树可对 `.md` 一键导出 `.docx`（云端与本地工作区同源能力）。
- 清空草稿区：工作区可清理临时草稿，避免干扰正式产物。
- 文件工作台与协作细节打磨：配合后端案卷目录 / 执行校验闸的桌面呈现。

### v0.6.9 — 2026-07-25

- 案卷阶段目录与执行校验相关 UI 对齐；发布链路与安装包侧小修（含 ripgrep 资源拉取兜底）。

### v0.6.8 — 2026-07-25

- 维护性发版：对齐后端检索预算与流式类型加固；内嵌 ripgrep 与文件工作台打磨。

### v0.6.7 — 2026-07-24

- 计划复核拍板：团队阶段性完成后会暂停请你决定「继续 / 调整 / 停止」；拍板中心展示主 Agent 把关意见（结论、风险、建议），对话流内仅保留一行「等你拍板」标记。
- 完成条件卡：交付未达标时诚实列出缺口，并提供「绑定本地文件夹 / 续写 / 续派页面验收 / 续跑未执行节点」等一键动作。
- 辩论庭前取证：开赛前可见各方取证进度与证据台账，不再一过卡就直接开辩。
- 本回合文件改动预览：产物卡可只读「查看改动」，便于核对本回合写了什么。
- 客户端过旧软提示：版本低于服务端要求时顶部出现可关闭提示，引导去更新。
- 登录会话更稳：生产环境经反代时刷新令牌路径对齐，减少 access 过期后莫名掉登录。

### v0.6.4 — 2026-07-20

- 权限模式切换有了对话内提示：切换权限档位后，聊天流里会出现一条居中的「权限模式 A → B」系统提示行，标明从下一回合起生效（此前仅在侧栏安全台账呈现）。
- 模型选择统一：配合「聊天框直接选模型」，移除旧的 fast / strong 抽象档位与思考强度分级，团队成员与你所选的具体模型一致。

### v0.6.3 — 2026-07-20

- 新增「你的规则」栏：可自建 / 编辑 / 重命名 / 删除你自己的规则文档（全局 + 分项目两层），与「AI 记忆」并列，复用同一 Markdown 编辑器（编辑 / 预览 / AI 改写 / 冲突保护）。
- 聊天框直接切换模型：输入框升级为模型选择器，多家 BYOK 服务商与平台模型混排、按服务商分组，显示能力 / 上下文 / 单价。
- 多服务商模型配置：设置页可同时接入 DeepSeek / OpenAI / Kimi / 智谱 / 豆包 / OpenRouter / 自定义，各自测连通，并设账号级「聊天默认 / 后台默认」。
- 内嵌网页预览 + 团队浏览器直播 / 接管：AI 产出的网页应用内完整预览；团队浏览器执行可实时观看并接管。
- 协作可视化增强：能力包、阶段卡、辩论见证等。
- 运行计时更准：「执行中 · Ns」锚定后端真实开始时间，折叠展开 / 刷新 / 切换后不再归零。
- 更诚实的成本估算与每日额度呈现。

---

## 后端 API（api）

### v0.7.5 — 2026-08-25

- 开工确认卡产品位拆除：新回合不再发卡；库存量续跑返回 410。组队 / 开辩编制到即开跑。
- 建站不再走具名套餐：改为手写交付 + 可选页面质量扫描；设计规范会注入到扫描工人开场。
- 工人可取消任务、工具参数与重建提示词对齐现行编制；代码执行改道闸不再进工具目录。
- 选错工具通道时（用跑代码搜源码等）过程事件记为 `redirect`，不再当失败。
- 会话日志列表改为瘦身载荷；新开辩论幕授权记为自动开辩（旧标记仍可读）。

### v0.6.5 — 2026-08-17

- 同一对话只保留一张开工卡：发新卡前会结算旧的 pending（含尚未落盘的 orphan），避免叠多张可点卡片。
- 折卡问句不再误清已有气泡正文：检查点只落时间线标记，只有引擎明确把本轮散文吸进卡片时才重置气泡。
- 真中断给出可读说明：重叠取消、达回复轮次上限、达回复长度上限等空收口会写「中断说明」；引擎主动早停（无进展等）仍空正文，语义由结束原因承载。
- 沙箱不再按语言预检探针：工具超时不再误判执行环境不可用；gVisor 回写改按内容指纹判断是否真写上，减少「说写好了其实没落盘」。
- 可观测性：事件环卡顿探针、SSE 空闲（距上一帧 / 心跳）检测；`/readyz` 修复 HOME / 证书导致的误判库挂；Redis 连接与读写设超时上限，避免卡住整进程。

### v0.3.36 — 2026-08-05

- 工作区文件列表可选返回大小与修改时间（向后兼容）。
- 回合触达轮次 / 预算硬上限时收口更诚实。
- 自有 Key：连通性检测与余额 / 鉴权 / 模型错误提示更稳；Kimi 预设默认模型更新。

### v0.3.21 — 2026-07-31

- 本机 MCP 通道：`mcp_op_required` SSE + 动态工具注册（经桌面 stdio 履行）。
- 常驻任务系统模板：`/v1/standing-task-templates*`、「每日对话回顾」卡与确认后 apply；迁移 `template_key` / `template_config`。
- 交付路径级验收：`delivery_status.artifacts`；主清单只认验收行。
- 热交互重挂：恢复会话时重放未决审批 / 授权 / 升级。
- 开工提案卡产品面下线；`file_read` 对 Office/PDF 透明抽文本；`code_execute` 同会话串行；工作区内部区路径化。
- 管理端用户详情：默认模型名改读账号「模型组合」（跟迁移漂移收口）。

### v0.3.20 — 2026-07-31

- 消息 IM：全站官方号广播（Admin 公告 publish → IM）；回复引用快照；结构化 @提及；在线态 presence / REST `online`。
- 权限与自治：用户默认自治配方（谨慎 / 少打断 / 托管）；深度研究自治旗标与托管蕴含；默认少打断跳过组团开工卡。
- 证据台账：回合台账字段扩展（`selected` / `doc_kind`）；成稿书目形态闸与验收 soft overlay 更诚实。
- 跟进推荐：`followups_unavailable` 事件；后台 chrome 平台 key auth 失败可一次回落 BYOK。
- 迁移：`d4a1…` 官方号、`e2b7…` reply_to、`f3c8…` mentions、`f3a7…` deep_research_auto、`e3b7…` autonomy_policy、`a6e2…` 清理旧 write_code 自治。

### v0.3.19 — 2026-07-30

- 辩论证据包 / 庭前取证：正式开赛前各方搜集与台账增量事件；取证不齐时诚实降级。
- Client Tool 续挂：恢复会话时可重新附着本机 / 白板 / 工作区等待履行操作。
- 运行时治理收口：引擎闸与 playbook 路径简化，团队收束与升级更干净。
- 安全依赖升级。

### v0.3.18 — 2026-07-29

- 本机 Host 工具与事件：`host_*` 工具面 + SSE `host_op_required`；桌面 Client Tool 回填。
- 权限第四轴 `host`：会话 `permission_axes` 缺省本机为每次确认（ask）；旧三轴载荷兼容补齐。
- 治理收口更诚实：无半成品时强制收束不再空跑 salvage LLM。
- 安全加固：MFA / 登录失败等敏感操作审计与限流；BYOK 相关加固延续。

### v0.3.17 — 2026-07-28

- 站立任务 L1 + Webhook：云工作区定时 / 钩子代跑 CEO 开轮；同任务互斥防双跑；拍板结束后收件箱状态收口。
- 权限三轴：会话存 `permission_axes`；运行时按三轴门控。
- 本机浏览器桥：Local Bridge 会话与桌面 WebContents 对齐；浏览器会话 REST / 活动事件。
- `code_execute` PYTHONPATH：默认注入工作区根与存在的 `src`/`lib`。
- 对话日志检索 / 记忆编辑等工具与执行闸加固。

### v0.3.16 — 2026-07-27

- 维护性发版：与桌面 0.6.11 / 手机 0.3.8 对齐；下载 CDN 与协调闸小修延续。

### v0.3.15 — 2026-07-26

- Markdown → Word：内置转换与工具，工作区 / 对话文件可导出 `.docx`（含图片与基础样式）。
- 下载分发：安装包 / Android 产物可同步到自有下载 CDN；官网下载链路对齐。
- 协调与执行闸加固：委派 / 能力策略与产物目录边界更严，避免缺能力时冒充探路。

### v0.3.14 — 2026-07-25

- 案卷阶段目录、执行校验闸与恢复清理；检索预算统一与 LLM 流式类型加固。

### v0.3.13 — 2026-07-24

- 计划复核前强制主 Agent 把关：暂停前附带结论 / 风险 / 建议；你放行后把关要点会带给下游。
- 辩论庭前取证：正式开赛前先各方搜集证据；取证不齐时诚实降级，不假装已备齐。
- 交付更诚实：空交接 / 明显缩水的上游正文会被拦住或标成缺口；完成条件未满足时结构化回传缺口与待办动作。
- 成篇与建站质量闸：调研报告与建站流程加强验收（含页面静态检查与可选视觉审阅），不合格会打回重做而非草草交卷。
- 分段写入成稿：多成员并行写章节后可按标记安全拼进主文件，减少互相覆盖与版式错乱。
- 生产登录刷新：刷新 Cookie 路径随反代前缀配置，修复「access 一过期就掉登录」的部署接缝。

### v0.3.10 — 2026-07-21

- gVisor 产物写回：沙箱内 bind/gofer 无法回写 host staging，改经 tmpfs `/workspace` 执行 + stdout artifact payload，由 API 进程落盘到 staging 后再 copy-out（根治 9p EINVAL）。

### v0.3.9 — 2026-07-21

- gVisor 产物写回：staged 运行改用 tmpfs `/workspace`（runsc 无法在 bind mount 上 mkdir）；执行前后经 `/workspace-seed`/`/workspace-sync` 与 host staging 同步；bundle 根目录在 `DATA_DIR/sandbox` 命名卷。

### v0.3.8 — 2026-07-21

- gVisor bundle/staging 根目录改到 `DATA_DIR/sandbox` 命名卷（避免容器 overlay `/tmp` bind 进 runsc 后 mkdir EINVAL）；OCI 交换面仍限 ephemeral 树 0777/0666 或 chown 65534。

### v0.3.7 — 2026-07-21

- gVisor 交换面权限：非 root API 容器对 ephemeral staging/scratch 使用 0777/0666（对齐 PoC 实跑路径），修复 rootless runsc 下 uid 65534 仍无法 mkdir 的 EINVAL/PermissionError。

### v0.3.6 — 2026-07-21

- gVisor 产物写回补遗：`/scratch` 脚本目录与 staging 一样在 copy-in 后对齐沙箱 OCI 用户权限；修复仅 stdout 可跑、带 `cwd` 写文件仍 PermissionError/EINVAL 的接缝。

### v0.3.5 — 2026-07-21

- gVisor 产物写回修复：staging 目录在 copy-in 后与沙箱 OCI 用户（uid/gid 65534）对齐权限；非 root API 容器回退为仅对 ephemeral staging 树授予 other+rwX，沙箱内带 `cwd` 写文件不再 PermissionError，copy-out 仍可读。
- 集成验证脚本：支持 api 镜像内 `/app` 布局跑 `--live`（不再 `parents[3]` 越界）。

### v0.3.4 — 2026-07-21

- gVisor 沙箱修复：修正 `runsc run` 全局旗标顺序（`--rootless` / `--network=none` / `--root=` 置于 `run` 之前）；启动健康探针改为真实最小 `runsc run` 冒烟（不再仅 `--version`），沙箱链路故障时执行类工具整类不装配。

### v0.3.3 — 2026-07-20

- 平台模型「一 key 一模型」中转：可为每个平台模型单独配置上游 API key 与 base_url（未配置的模型回退共享默认），计费仍按平台口径入账；配置格式出错时安全降级到共享默认。
- 统一模型编排：移除 fast / strong 两档模型偏好与思考强度（reasoning_effort），队员统一使用单一 agent 档；事件契约（PlanAgentPayload）与一致性向量同步收敛。

### v0.3.2 — 2026-07-20

- 多服务商 BYOK：一个账号可同时接入多家 OpenAI 兼容服务商（各自 key / base_url / 默认模型 / 价卡），账号级「聊天默认 / 后台默认」指针。
- 统一模型目录：按每回合所选模型的来源做计费路由（平台 vs 自带 key）。
- 文档子系统第一期：documents 载体表 + 用户规则闭环 + AI 记忆迁入文档表 + 跨文件注入预算。
- 成本防线：每日成本配额 / 每月免费额度。
- 团队浏览器 L3：内嵌浏览器实时直播 / 接管。
- 能力包、辩论证据台账（findings / witness）、research-first 运行时、阶段卡。
- 诚实交付记账：worker 超时通知与「交付缩水」解耦；files_touched 只认执行成功的写盘。
- 诚实搜索质量信号：整批弱结果显式警告、硬拦域名出口切分、弱结果自动重试。

---

## 手机端（mobile）

### v0.4.12 — 2026-08-25

- 跟桌面同一套产品页：组队 / 开辩不再挂开工确认卡；助手回复的工作区图片对比围栏同样可用。
- 独立手机网页站已下线；原生壳仍走 Capacitor 本地源连 API。

### v0.4.4 — 2026-08-17

- 同一对话只保留一张开工卡：冷启动恢复与协议折卡对齐后端投影，避免叠多张可点卡片。
- 折卡问句不再误清已有气泡正文：检查点只落时间线标记，与后端契约同步。

### v0.3.26 — 2026-08-05

- 对话「＋」可改本会话权限与模型组合，并可设为新会话默认。
- 文件列表显示大小与最近修改时间。
- 助手消息脚注与桌面对齐（费用 / 轮次 / 用时）。

### v0.3.13 — 2026-07-31

- 协议跟发：交付文件卡按路径级验收展示；恢复卡 / 团队交付视图对齐。
- 开工提案专用壳跟桌面一并下线（本机 MCP / 日回顾仍仅桌面）。

### v0.3.12 — 2026-07-31

- 消息 IM：官方号只读会话；回复引用展示与发送；@ 高亮 / 被 @ 描边（完整 @ 菜单与在线态本版不做）。
- 自治设置：谨慎 / 少打断 / 托管配方与桌面对齐。
- 错误文案与跟进不可用标记跟发。

### v0.3.11 — 2026-07-30

- 协议跟发 · 辩论庭前证据包：辩论视图展示庭前取证进度与证据台账增量（本机履行仍仅桌面）。
- 安全依赖升级。

### v0.3.10 — 2026-07-29

- 协议跟发：对齐 Host 权限轴与 `host_op_required`（本机操作仍仅桌面；parity 标 impossible）。
- 权限配方展示接第四轴；法律文案同步。

### v0.3.9 — 2026-07-28

- 协议跟发：对齐本波权限三轴 / 升级写权字段；站立任务仍仅桌面（parity 有意不接）。
- 消息图片画廊：会话附件图可点开预览 / 分享。
- 自主度设置文案与权限配方展示微调。

### v0.3.8 — 2026-07-27

- 维护性发版：与桌面 0.6.11 / api 0.3.16 对齐。

### v0.3.5 — 2026-07-24

- 协议对齐 · 计划复核：挂起后可用续跑卡「继续 / 调整」，与桌面拍板语义一致。
- 协议对齐 · 辩论庭前取证：辩论视图展示庭前取证状态与证据台账条数。
- 协议对齐 · 完成条件：团队交付未满足时展示缺口摘要（手机端无本机文件夹绑定，相关项仅作提示）。

### v0.3.3 — 2026-07-20

- 协议对齐：团队视图与协议 fold 跟随后端移除 fast / strong 模型档与思考强度分级，与桌面 / 后端保持一致。
- 维护性同步：随本波多端协议收敛做对齐（补齐桌面「权限模式切换提示行」的手机对等裁决——手机可在自主度设置切换档位，暂不在对话流内渲染该提示行）。

### v0.3.2 — 2026-07-20

- 多服务商模型配置：模型设置重构 + 新增服务商表单，手机端也能接入 / 管理多家 BYOK 服务商。
- 来源可信度徽标 + 证据台账渲染：引用按官方 / 媒体 / 待评 / 弱源分级展示。
- 对话与工作区体验增强：对话页交互优化、工作区暂存目录、用量设置。
- 协议对齐：新增团队浏览器直播事件处理与契约 parity 补齐，与桌面 / 后端保持一致。

---

## 管理后台（admin）

### v0.3.8 — 2026-08-25

- 会话复盘补大纲与输入框幽灵稿，空助手气泡会折叠，便于顺着回合看。
- 历史上的开工卡在复盘里标成已退役，只显示种类与状态，不能当活卡继续。
- 选错工具通道（用跑代码搜源码等）在过程里标成「改道」，不再当失败。

### v0.2.5 — 2026-07-31

- 产品公告：展示面文案对齐「publish → IM 官方号 / 横幅」；协议与后端同波。

### v0.2.4 — 2026-07-30

- 协议跟发：随本波后端契约同步；无用户面大改。
- 安全依赖升级。

### v0.2.3 — 2026-07-28

- 协议跟发：随本波后端契约小改同步部署；无用户面大改。

### v0.2.2 — 2026-07-20

- 用户详情下钻增强：已配服务商数、近 30 日 by-model 用量、最近会话与回合活动。
- 分析看板增强：by-role / by-model 用量、观测 feed。
- 运营工具：配额管理弹窗 + 一键复制 ID 组件；会话复盘 / 系统页增强。

---

## 官网（website）

### v0.2.12 — 2026-08-25

- 下载页组件更名对齐现行 `DownloadView`；版本钉在桌面 / Android 本波发布之后再 bake。

### v0.2.6 — 2026-07-31

- 下载页 `FALLBACK_VERSION` 对齐桌面 **0.6.16**（须桌面 Publish 后再部署官网）。

### v0.2.5 — 2026-07-30

- 下载页 `FALLBACK_VERSION` 对齐桌面 **0.6.15**（须桌面 Publish 后再部署官网）。

### v0.2.4 — 2026-07-29

- 下载页 `FALLBACK_VERSION` 对齐桌面 **0.6.13**（须桌面 Publish 后再部署官网）。

### v0.2.3 — 2026-07-28

- 下载页 `FALLBACK_VERSION` 对齐桌面 **0.6.12**；随本波桌面 Publish 后部署。

### v0.2.2 — 2026-07-26

- 下载页对接自有 downloads CDN；桌面 / Android 资产与 `FALLBACK_VERSION` 对齐至 0.6.10。

### v0.2.1 — 2026-07-20

- 维护性发布：下载页桌面版本回退值同步至 0.6.3；构建 / 预览脚本小幅整理。无面向访客的功能变更。
