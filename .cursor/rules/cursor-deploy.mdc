---
description: 全端部署（后端+官网+手机+管理后台+桌面）——用户说「部署/上线/发版」时读；凭据存 deploy/.env.deploy.local 不入仓。
globs:
  - "apps/website/**"
  - "apps/mobile/**"
  - "apps/admin/**"
  - "apps/desktop/**"
  - "apps/server/**"
  - "deploy/**"
  - ".github/workflows/*"
alwaysApply: false
---

# 部署规范

完整操作手册 → [`发布与门禁.md` · 本地发布操作手册](/docs/05-平台与运维/发布与门禁.md#本地发布操作手册)。入口壳 → [部署与运维](/docs/05-平台与运维/部署与运维.md)。公告政策 → [产品公告文案模板](/docs/05-平台与运维/产品公告文案模板.md)。

## 铁律

- **凭据禁令**：`deploy/.env.deploy.local` 不入仓。禁止在 chat 索要/回显 token、SSH 私钥或其它部署凭据。
- **不重新启用** deploy/release workflow 的 push/tag/schedule 自动触发。
- **准备 ≠ 切流**：`pnpm deploy:backend <sha>` 只预构建（worktree、不 checkout 活树、不推 `latest`、不停 api）。切流 `pnpm deploy:backend:switch <sha>`。紧急一把上 `pnpm deploy:backend:now <sha>`。
- **「部署」只授权准备段**：用户说部署/上线/发版，做到 CI 绿 + 预构建 + 桌面/Android draft + Mac GHA 即**停**。`--switch` / `--now` / Publish / admin·web·website **必须再问**「现在空闲、可以切吗？」——得到「切 / 空闲了 / 现在上」才继续。禁止把「部署全部改动」「同一坐席」理解成立刻停 api；禁止拿夜深当空闲。空闲 = 人确认没人在跑对话/任务。未点名紧急不得 `:now`。
- **新增/改接口 → 后端必须先切流**。前端 deploy 脚本契约门探的是线上 `/version`，预构建不够。
- **协议 / UI 混改**：准备可提前；**人确认空闲后**同一坐席切（api + admin + desktop + Android + website）。同船内后端仍先 `--switch`。
- **桌面发版**：Win = 本地 `release:win`；Mac = GHA 仅 mac；**勿用 GHA 打 Win**。准备段停 draft，切流段才 Publish。
- 发布前本地一键门禁：`pnpm release:gate`（含 schema gate）。日常可用 `:lite`；**发布仍须全量**。
- **预构建 / 切流前须等 `ci.yml` 全绿**（本地 gate 不含 integration、且 Win≠Linux）。
- **后端切流顺序**：停旧 api → `alembic upgrade` → schema gate `--live` → 起新 api。禁止旧容器接流量时跑破坏性迁移。`finish-server.sh` 内核不另写一套。
- **紧急续跑 finish**：镜像已在机上、finish 因 `/data/logs` 权限失败 → `node deploy/scripts/resume-finish.mjs <short-sha>`。不是主路径、不是 npm script。
- **默认不发预告**。全端切流验收后发收口；预告仅破坏性迁移 / 无法空闲切。勿绑进 deploy 脚本自动发。

## 全端顺序（速查）

半自动清单：`pnpm release:ship`（`--track api` = 仅后端；`--check` 探 draft / updater feed）。

```text
pnpm release:gate
# bump → commit → push
gh run watch <id> --exit-status
pnpm deploy:backend <sha>                          # 准备；桌面/Android 打 draft
# 空闲后同一坐席：
pnpm deploy:backend:switch <sha>                   # 切流
# full：桌面/Android Publish → admin / web →（桌面 Publish 后）website deploy:pages
pnpm release:notice -- --phase done …               # 收口；全端必发
# tag prod-* / desktop-v*
```

独立 `m.` 站已下线，勿再上传手机 Pages。
