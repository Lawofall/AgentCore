---
description: 仅当用户贴 32-hex / UUID / 排查包，或明说查产品 AI 失败 / 巡检时读。讨论产品或架构默认不打开。有 ID 只跑 CLI，禁止写脚本 / 扫 jsonl / 搜仓库。
alwaysApply: false
---

# 对话日志查询

## 何时打开（默认关）

满足任一才往下查：贴了 32-hex / UUID / 排查包；或明说查这次失败 / 巡检 / reviews。其余讨论（产品、架构、拦截、取舍）→ 不 sync、不 patrol、不 `--recent`。打开后只答「这次发生了什么」；驱动改动须码核 HEAD；与已确认设计冲突时日志让路。按问题选证据 → `dev-process.mdc`。

## 两套日志，不要混

| | 产品 AI 日志 | Cursor IDE Agent 会话 |
|---|---|---|
| **位置** | `logs/dev.jsonl` + Postgres | `agent-transcripts/`（UUID.jsonl） |
| **ID** | 32 位 hex 无连字符 = **trace_id**；带连字符 UUID = **conversation_id** | 文件名 = UUID 带连字符 |

用户给 id → **`logs/dev.jsonl` + DB**。`agent-transcripts/` 与产品 AI **无关**。日志**不含消息正文**（在 Postgres）。

## 查询入口（`apps/server` 下；勿先 Glob 脚本）

```bash
uv run python scripts/log_timeline.py --trace <trace_id>          # 默认 decision_spine
uv run python scripts/log_timeline.py --pack <dir> --trace <tid>  # 排查包
uv run python scripts/log_timeline.py --pack <dir> --full --trace <tid>
uv run python scripts/log_timeline.py --raw --trace <trace_id>    # 全量仅需要时
uv run python scripts/log_timeline.py <conversation_id>
uv run python scripts/log_timeline.py --recent
uv run python scripts/log_stats.py --json
uv run python scripts/log_patrol.py --export-dir ../../logs/prod-export --since 2d  # 巡检①
```

有 ID → 优先 spine / `--pack`；`--raw` 仅深挖。包契约与 journal-only 自动存档 → [分析指南](/docs/05-平台与运维/对话日志分析指南.md)。`spine_events` = **turn_spine**，勿与产品面混称。

## 最短路径（已打开时）

**禁即兴**：禁止 Write 脚本、`python -c`、`jq`、Grep/rg jsonl、在仓库搜 ID、Glob 定位 CLI、派探子、整篇读分析指南。CLI 不够 → `--pack` / `--raw`；仍不够 → 停、问人。

单条 ID（即便夹讨论根因）→ 主自跑上表，cwd=`apps/server`，不套分相。缺正文再 `--pack`（必要时 `--full`）。本地 0 事件且像线上 → `pnpm sync:logs` 后同一条加 `--export-dir ../../logs/prod-export`；仍空 → 停并报空，勿换工具。勿再用 `AGENTCORE_SSH` / `$AGENTCORE_HOME/logs`。

巡检 / reviews（仅用户明说）→ [`logs/reviews/README.md`](/logs/reviews/README.md)：① `log_patrol.py`（禁复制扫描脚本）② 按密度拆只读桶。写成「建议改」前须码核 HEAD。

假卡 / 本机横幅 / 缺窗 / token 空才开 [分析指南 · 症状进层](/docs/05-平台与运维/对话日志分析指南.md#症状进层)。Dogfood → [`evals/dogfood/README.md`](/evals/dogfood/README.md)。

## 查询策略

- **无连字符 32-hex** → `--trace`（勿先当 conversation_id）
- **带连字符 UUID** → conversation_id；全貌用位置参数，单轮再 `--trace`
- **无 ID** → 未明说查日志则不打开；仅当用户明说「查最新」才 `--recent`，须用文案/`preview` 确认匹配
- **ID 已齐** → 直接 `log_timeline --json` 或 `--pack`
