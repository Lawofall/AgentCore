---
description: 改产品 AI 提示词——宪法非法例；入场闸只选一层；禁字面测试；瘦身不换闸
alwaysApply: true
---

# 提示词改动

产品 AI 提示词（CEO 核 / 基座 / 队员身份 / 辩论 / Skill / 工具 schema / 事实层 / 场面门 / 引擎当场文案）**先读再改**：[上下文工程 · 提示词设计原则](/docs/03-AI核心/上下文工程.md#提示词设计原则)。本文只留 How。加闸 → `intercept-discipline.mdc`，禁止用拦截补短提示。

## 入场闸（加一行只选一个落点）

1. 现有原则已覆盖 → **只加行为回归**（conformance / eval / `test_closing_posture`），不改常驻字面。
2. 罕见场面 → 场面门 / 一次性尾（先事实，姿势一句）。
3. 按需 HOW → **`consult` 正文**，不进常驻核。
4. 参数约束 → schema 短触发（`HOW→consult(name)`；参考 `debate`）。
5. 本回合环境 → `<workspace_context>` **只陈述事实**，不写禁令/剧本。
6. 新的常驻**路由脊柱** → 在唯一所有者加一句；棘轮抬顶须在 PR 说明（不是回潮）。

## 一层一所有者

事实行不写 HOW；基座不写 CEO 路由；`how_you_work` 不写能力怎么动（那是 `how_you_act`）；核不写进阶手册；skill 目录行只写何时拉；队员叶子不抄 CEO 路由 / 全员诚实元规则（captain 嵌套 lead 短判决住 identity，目录不挂编排 skill）；辩论不点名个案查询词、不复述 `web_search` 词数。详表 → 原则文所有者表。

## 禁止

- 常驻当事故日志：会话 id、具体查询词、禁语近义词表、某次失败原话。
- 新加「某句禁令必须出现在 `_CEO_CORE_HINT`」。`test_prompt.py` 守原则 / 缺席 / 唯一性，不守判例字面。
- 把路由脊柱（CEO 自判、短问、先取证再开口、探路≠摸底、编制按缝）赶进 `consult`。
- 两棵树共用 ①–⑤；`how_you_work` 写能力怎么动 / 对用户怎么说（那是 `how_you_act`）。
- 瘦身同时加硬闸 / 累计软提醒 / 扫用户长文的意图分类器。
- 同一 HOW 在核 / skill / schema / 事实层再抄一份。
- 注入字面出门过 [现行信息](/docs/01-产品/现行信息.md)：不改变以后行动的句子不留；废名连删，测试改缺席，禁止靠点名废字段「禁复活」。

```
# ❌ 核里加「禁止说现象已消除」并断言字面必须在场
# ✅ 原则已覆盖 → 只加收口结构测试；棘轮只降不升
# ❌ list_folders description 再写一遍跨文件夹十步
# ✅ 短触发 + HOW→consult(team_cross_folder)
```

体积：`tests/test_ceo_core_ratchet.py`（基座+核）、`tests/test_tool_schema_size_ratchet.py`（工具面）。默认只降不升。
