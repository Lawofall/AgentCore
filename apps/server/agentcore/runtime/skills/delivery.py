"""Skill body: delivery (用户点名的文件格式 / 真 Office / 产物路径)."""

from __future__ import annotations

_DELIVERY = """\
<交付环境>
用户点名的文件格式、真 Office、产物路径——本条。对照 `<工作区>`（含「缺口：」）与本回合工具表。

【用户点名的格式】只认本回合工具表 ≠ 凭印象猜导出器。\
表上有 `md_to_docx` / `md_to_pdf` → 交真 `.docx` / `.pdf`（与执行正交：落盘 `.md` 后导出）\
≠ 静默降成 `.md`/脚本、≠ 因「缺口：」有 `run` 就说做不到、≠ 用 `run` 顶替这两把确定性导出器。\
工具表无对应导出器、且「缺口：」含 `run` → 该格式本回合不可产：≠ 再派「跑脚本」空转。\
有等效替代 → 先干完不依赖该项的活，写清做不到什么与升级路径，升级走非阻塞追问。\
本条先于开场提问的载体·手段向用户发问。\
无等效替代 → `ask_user` 只覆盖这类目标，勿把工具表已有的导出器说成做不到。\
用户明示当模板 → 先 `file_copy` 再改 ≠ 空白新建。\
「缺口：」含 `package_install` 时，工作区里能过的检查 ≠ 外环已跑通；写明未装包，或 `export_to_local`。\
交可打开的表时把表质量基线写入 task，勿用章节清单冒充表结构。\
其它无执行交付 → 落盘并标交付缺口，或只报告、不落盘。

【路径】讨论/调研/审查类交付写 `AgentCore/文档/`：有专属出口才进 `research/`、`debate/`、`reviews/`；\
其余自定位；不知放哪再进 `工作稿/`。用户工程源码仍写业务路径。\
【产物路径】向用户列落盘须工作区相对**完整**路径（与约定文档出口同前缀）；\
以本回合写盘回执 / `deliverable.artifacts` / 交付对账为准 ≠ \
缩短成裸 `reviews/…`、同一清单混用两套前缀、或报未写入的路径。\
收口按工作区相对完整路径说 ≠ 讲「归位 / 移到工作区」或写完再搬。
</交付环境>"""
