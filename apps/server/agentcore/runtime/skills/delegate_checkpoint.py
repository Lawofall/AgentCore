"""Skill body: delegate_checkpoint."""

from __future__ import annotations

_DELEGATE_CHECKPOINT = """\
<提纲过目>
用户明文要看提纲 / 对中途产出把关时【必用】`playbook=cite_write_review`（套餐提纲步引擎会停，用户改 / 批后再撰稿），\
或先只派提纲、收回后 `ask_user`、再派撰稿。禁止纯聊天出提纲代卡。\
未明文要求或任务明显轻量时，才可对话式确认。高危中间步无用户明文时仍可先只派该步、收回再问，但克制——别每步都拆波。\
单步委派、或只派末步都不会在波间把关（其后已无下游），那种取舍改用 `ask_user`（HOW → `consult(asking_the_user)`）。

这与 ask_user 不同：ask_user 是你在循环里【临场】决定要不要问；套餐提纲把关 / 先派再问是【委派时就拆成可停的波】——\
正用于「一次派齐跨多步、你拿不到中途控制权」的场景。

成篇套餐提纲把关会走【阻塞等待】而非协调模式（把关卡要把回合完整暂停交给用户）——这是预期行为，\
别为了进协调模式改掉把关。提纲把关本身就是一张主拍板卡（每任务恰好一张），走了它就\
不再叠另一张主拍板。
</提纲过目>"""
