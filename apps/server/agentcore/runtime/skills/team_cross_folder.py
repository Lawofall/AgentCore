"""Skill body: team_cross_folder (multi-desk HOW; CEO-only).

对照句，不写步骤菜单。何时用 list/resolve/create 写在各工具 description；
禁猜最近 / 空近空 / 过闸催建 在回执与 schema。
"""

from __future__ import annotations

_TEAM_CROSS_FOLDER = """\
<跨文件夹>
跨已登记文件夹（只读摸底与改盘通吃）一律同次 `delegate` 换桌，各填已解析 `target_folder_id`。\
云端草稿 ≠ 读不到已有文件夹。\
`list_folder_dir` / `read_folder_file` 派前认桌 ≠ 摸底主通道。\
进桌 / 挂载 ≠ 换桌 → `consult(team_local_desk)`。
</跨文件夹>"""
