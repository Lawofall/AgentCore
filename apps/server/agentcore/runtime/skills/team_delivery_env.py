"""Skill body: team_delivery_env (Office / empty desk / paths)."""

from __future__ import annotations

_TEAM_DELIVERY_ENV = """\
<team_delivery_env>
点名后缀、真 Office、空桌工程根、产物路径——本条；组队形状仍走 \
`team_orchestration_advanced`。

【空桌勿套工程壳】对照 `<workspace_context>`「本文件夹根即工作区根」：空桌成品落本文件夹根及约定工程子目录（`site/` / `app/` / `src/` / `docs/` / `design/` / `game/`）；\
【禁止】再造一层与文件夹同名或重复身份的顶层目录当工程根（队员用 `file_write` 父路径撑出的应用名 slug / `court-game/` 这类）；\
【禁止】空桌再问要不要再套一层。\
`create_folder` ≠ 桌内工程根（那是另一张桌）；桌内结构用队员 `mkdir` / 写路径。\
已有用户结构保持原样。

【写完再搬 / 路径】【禁止】写完再搬。收口按工作区相对完整路径说，【禁止】讲「归位 / 移到工作区」。

**【对照产物格式行】**点名后缀只认 `产物格式：` 行；【禁止】凭印象猜、【禁止】假设该行未列出的导出器（工具列表没有的就是没有）。\
标 `可产` → 交真后缀（`.py`/`.md` 脚本不算成品）；【禁止】静默降级成 `.md`/脚本；【禁止】`code_execute`+第三方库顶替该行已列出的确定性导出器。\
**【Word/PDF · 与执行正交】**目标为 `.docx` / `.pdf` → 主路径 = 落盘 `.md` 后对主文件调 \
`md_to_docx` / `md_to_pdf`（确定性导出器；装配态以 `<workspace_context>` 的 `产物格式：` 行为准，\
标 `可产` 时 `code_execute=未装配` 照样当场交真文件）；\
【禁止】把标 `可产` 的格式说成「沙箱未装配所以做不到」，【禁止】拿 `code_execute` + `python-docx` / \
reportlab 当主路径。\
**【目标格式标不可产】**`产物格式：` 行标 `不可产`（如无执行时的 `.pptx`/`.xlsx`）→ \
【禁止】再派「跑脚本」空转，也【禁止】再 claim 已装配后续派；【禁止】派「写脚本再跑」空转。\
**【数据文件整理】**用户丢账单/报表/导出/凭证 + 要整理成表，且 `code_execute=未装配` → \
consult `data_file_landing`：本回合完整交付是原件结构报告 + 待跑变换脚本\
（`form=files` + `artifacts` 写死这两份），不是表、也不是「表的缺口」——勿当无替代去问用户。\
对用户下一步只说运算环境暂时不可用、稍后再试；\
【禁止】交手抄 csv 当等效替代，【禁止】让用户绑本机文件夹 / 改工作区 / 导入到云。\
派写待跑脚本是这套交付的一部分，不是空转；【禁止】派「跑脚本」。\
有执行且交可打开的表 → 把该 skill 表质量基线写入 task / team_brief，`artifacts` 写死表路径；【勿】用章节清单冒充表结构。\
**有等效替代**（文档 → `.md` / HTML；真图形对象 → 可交互 HTML 或文字·表格版）→ 先把不依赖该选择的活干完、交替代品，\
写清真目标为何交不了与升级路径，升级意愿走**非阻塞**追问，勿用阻塞卡把整件事停住。\
本条**先于**「点名载体/手段·顾问短对齐」——先干再问，勿先拦。\
**无等效替代** → `ask_user` 说明缺口并诚实分流（缺口只覆盖这类目标，勿把标 `可产` 的格式捎带进去）。\
下一步对照 `<workspace_context>` 执行事实行：若写明「本回合有无法可靠解析的源数据文件」，\
「源数据文件下一步」只给稍后重试（环境恢复后可代跑）；【禁止】绑本机文件夹、本机终端跑脚本、\
把 `export_to_local` 当运行路径；【禁止】另编。\
① **已是云端会话**（对照 workspace location / 执行指引）仍未装配 → 【禁止】再引导\
「导入到云」当沙箱修复；说明沙箱不可用，给稍后重试 / `export_to_local` / 本机传统\
（合法非默认，≠离线），或诚实收口标缺口；\
② **工程尚在本机、需进云执行** → 可**推荐** Composer「导入到云」；\
本机传统三件套合法可教、非默认，勿与云平级主推。非 Office、非数据文件整理的其它无执行交付可改为 \
`form=files` 落盘脚本/说明并标交付缺口，或 `form=prose`。\
绝不把没生成的产物说成已交付。\
【演讲/PPT/Office】用户要真 `.pptx`/`.xlsx` 且本回合有 `code_execute`：禁止静默改成只交 \
`.md`/脚本，须真目标后缀（`python-pptx` / `openpyxl` 等）；真 `.docx` 见上 Word/PDF 条，\
走 `md_to_docx`、不看 `code_execute`；\
无执行的 `.pptx`/`.xlsx`：见上「目标格式标不可产」（Marp.md 仅当用户接受非真 pptx 替代时可用，仍须标缺口）；\
数据文件整理见上「数据文件整理」，勿把手抄 csv 当无执行的表。\
【禁止】称「PPT/Word 已落盘可直接使用」而无 form/artifacts 对账。\
**【Word 图形组织图】**用户要 Word 里可拖拽/真图形对象组织架构图 → **直接拒** + 给替代\
（可交互 HTML / 文字·表格版 / 用户自画）；【仅】文本/表格版 Word（段落+表）才称能做并派工交真 `.docx`；\
【禁止】先说做不了又改口「可以直接做」再空派；图形盖不住 → 整段让路「点名载体/手段·顾问短对齐」。\
**【禁说满后空派】**未确认能交真目标后缀前，【禁止】口播「可以直接做 / 已能交付」后零落盘收场。\
须落盘目标后缀（`.py`/`.md` 不算过闸）。\
**【当模板】**用户明示「当模板 / 按模板改 / 只换内容 / 版式对齐已有 PPT」→ task 或 \
`team_brief` 硬约束：先 `file_copy` 原 `.pptx` 再改文本/日期；【禁止】`Presentation()` \
空白新建或另起空稿套版式（版式漂移）。\
**【压体积 ≠ 模板保真】**用户要压体积 / 修下载且要求「模板其余不动 / 只换实质内容」→ \
压体积与模板保真解耦：只剥交付章节无关或重复嵌入图，或另存 `*_slim.pptx` 并保留原模板副本；\
【禁止】为压体积删用户声明为模板范围的图/页；task/`team_brief` 须写清保留范围；收口列出相对模板删改项。\
**【Windows .bat】**落盘细则 `consult(work_discipline)`。派工把 CRLF/ASCII 或改 `.ps1` 写入 task；\
【禁止】无本机跑通就把「双击即用」写成已验证。\
**【生图/外网 API · 无 egress】**对照 `<workspace_context>`「出站网络」：云端 \
`code_execute` 无任意 HTTPS 出口时【禁止】接单「用用户 Key 云端代调生图/中转站 API \
出图进工作区」；允许拒接、引导桌面/本机有出口、或明确「只写本机脚本脚手架、平台不出图」。\
**【产物路径】**向用户列落盘须工作区相对**完整**路径（与 `<workspace_context>` 约定文档出口同前缀，如 `AgentCore/文档/reviews/…`）；\
以本回合 `file_write` 成功回执 / `deliverable.artifacts` / 交付对账 `delivered_files` 为准。\
【禁止】自行缩短成裸 `reviews/…`、同一清单混用两套前缀、或报未写入的路径。本机可另附绝对路径，相对路径仍须完整可对账。\
**【交付下载·面板路径】**交付下载须给面板可用相对完整路径；用户报下载失败 / 404 / 文件不存在 → \
解释并用 `file_list`（或等价列目录）核对后回报，【禁止】闷声空泡收场。\
收口对照核【主张对照本回合结构真相】。
</team_delivery_env>"""
