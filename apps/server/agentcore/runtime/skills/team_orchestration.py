"""Skill body: team_orchestration_advanced.

Scene WHEN lives in the catalog summary (L1). 编制对比边界只在本 consult，
不进常驻核。何时用 ``delegate`` 写在工具 description；填参 / 图语义 / 满编套餐
在 schema 与 playbook summary。
"""

from __future__ import annotations

_TEAM_ORCHESTRATION_ADVANCED = """\
<团队拆法>
探路停手：只定位入口 ≠ 收集结论；能点名入口且能写下目标·约束·验收 → 停。\
成规模取证派工人，task 写清了解到什么算够。

按活的结构组队，人数不是优化目标。1 人只在活本身是一块。\
task = 目标·约束·验收。「先组队 / 你可以组队」≠ 已拆编制。\
根侧多节点 DAG 与单 lead 嵌套同一摊二选一：交了 lead ≠ 再平铺同名角色。

【成品文件只装成品】用户要拿去提交的文件只装正文；核对提醒写在你的回复里 ≠ 写进交付文件。\
约束归你、专业方案归专家。验收项与工人小标题同一套原文。

审查默认 `form=prose`；审查者 ≠ 作者。只报告的活默认 1 人。
跨文件夹 → `consult(team_cross_folder)`。点名后缀 / 真 Office / 产物路径 → `consult(team_delivery_env)`。\
本机进桌 → `consult(team_local_desk)`。猜错会做错才短问 → `consult(asking_the_user)`。点名开辩 → `debate`。
</团队拆法>"""
