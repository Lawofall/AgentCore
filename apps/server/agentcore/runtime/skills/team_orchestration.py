"""Skill body: team_orchestration_advanced.

Scene WHEN lives in the catalog summary (L1). 编制 / 探路 / 成文 HOW 只在本 consult，
不进常驻核。何时用 ``delegate`` 写在工具 description。
"""

from __future__ import annotations

from agentcore.runtime.coordination.session import (
    DEFAULT_COORDINATION_BUDGET,
    MAX_COORDINATION_BUDGET,
)
from agentcore.runtime.runs.playbooks import available_playbooks
from agentcore.runtime.runs.playbooks.audit import (
    CODE_AUDIT_REQUIRED_SECTIONS,
    CODE_AUDIT_SECTION_BY_DESIGN,
    CODE_AUDIT_SECTION_DEFECTS,
)
from agentcore.workspace.stage_dirs import RESEARCH_DIR, REVIEWS_DIR

_PLAYBOOK_LISTING = available_playbooks()
_CODE_AUDIT_SECTIONS_JSON = (
    "[" + ", ".join(f'"{s}"' for s in CODE_AUDIT_REQUIRED_SECTIONS) + "]"
)

_TEAM_ORCHESTRATION_ADVANCED = """\
<团队拆法>
【工作流】探路停手 + 决策层委派。下文形状词汇 / playbook 是对照，不替代本段。\
探路只回答「从哪几个入口进」（定位入口，不是收集结论；探路 ≠ 摸底）。\
停手：能点名入口（路径 / 符号 / 文档节）且能写下目标·约束·验收、执行层可留给工人。\
还不知道从哪进 → 再取证（取证 ≠ 必须开组）。方案层未定且选错会做错 → 挡路拍板或先不派，不是再搜。\
窗口里入口已在（本轮已打开、用户点名、画像 / 导航、上一气泡已钉）→ 探路已结束。\
再搜一轮若不能改变入口名单或三件套 → 停；再搜是为了把实施写进 task → 停。\
成规模取证交给工人，task 写清「了解到什么算够」。\
委派 task = 决策层：目标（本轮桌上结果）+ 约束（已拍板项 + ≤2–3 入口锚点）+ 验收（怎样算完）。\
自洽 ≠ 代写实施。不写逐步改法、补丁正文、章节骨架、书单 / 全仓 grep 清单。\
体积：简单几十、中等一两百、复杂不超过约五百 token——超了通常在做实施。\
入口已在 task / 主管探路 → 工人读锚点就干、执行层自解、方案层 escalate、写完即交。

【何时派】改产物 / 成规模取证该派就派（桌上结果已定之后）。何时用 `delegate` 见该工具 description。\
先想形状再拆任务——教的是【词汇 + 组合】，不是成品模板。自检：换个主题，形状还一模一样吗？还一样就错了。\
「调研+写码+点评+合成一篇」这类跨域合成流水线 → 常见 1～2 人，勿默认每人一种专长；\
档 3 成篇满编的【产出→独立审校】是质量缝、不算凑工种；普通构想 / 档 1–2【不】默认学术审校。\
跨文件夹 → `consult(team_cross_folder)`。空桌工程根 / 真 Office / 产物路径 → `consult(team_delivery_env)`。\
做软件手写顶层 `tasks`（HOW → `consult(building_software)`）。单页一人做完；控制台别套营销皮；\
HTML 落盘即自动静态质检。

形状词汇（按任务结构选、可组合）：
- 并列对象分组：每对象一员（重档升 lead 内拆维度），尾挂横向汇总
- 角度扇出：N 角度并行调研 / 产出，汇入下游
- 证据驱动流水线：调研 → 结构定稿（主拍板）→ 产出
- 独立审查：审查者 ≠ 作者，产出结构化问题清单
- 有界返工环：审查发现 → 原作者带现场续派修订 → 复核，≤2 轮，到限交代缺口；\
修订按审校意见逐条用 `str_replace` 局部改（优先）、扩写用 `file_append`；\
整文件 `file_write` 覆盖允许但勿惰性省略中段（反例：「……（中间省略，已保留首尾）……」致残缺交付）
- 契约共享面：`team_brief` 写清口径 / 接口 / 约束
- 独立多透镜诊断：对既有材料 N 透镜并审 → 风险分级汇总 → 普通 ask_user 多选勾选要处理项 → 定向修订
- 部件一致性对账：并行部件起草 → 对账节点专查接缝 → 定向修订
- 对抗辩论：主持人驱动交锋（何时开见 `debate` 工具 description）
- 发散挑选：N 风格并行 →（轻量互评）→ 普通 ask_user 单选让用户挑一个 → 中选深化

组合：多对象+成篇 → 分组×流水线；构建+并行模块 → 契约共享面+独立验证；\
审查要改 → 接有界返工环；跨域合成一篇 → 少派串起，勿按工种堆人。

三档：默认中档。轻=保底（能 1 人则 1 人；独立验证不是修码 / 轻构建的保底编制）；\
重=任务规模大或用户点名才上。控税靠选档与按缝拆人，不靠默认单干、也不按工种凑满。

【切片与两段】「先设计再实现」小 CRUD / 骨架 → 默认 **真两段** / **1 人两段**\
（wave1 设计/API + `form=files` 交回后再实现；或同批 ≥2 tasks + `depends_on` / \
同人 `continue_from_run_id`）。**【假两段·禁】**同一 task 写「先交设计再实现」或「先摸底再审计/再实现」。\
前端 UI / 壳层 / **桌面壳 / 多进程** / **多屏 UI / 单文件大原型** → 同默认真两段，或 wave1 只交设计；\
第一棒 ≠ 「设计 + 双子系统 + 壳层 + build / 可跑闭环」；首 grant ≠ 「完整可玩 N 屏」\
（桌上档 / `playbook_args` 等结构槽已点「一次做完」除外）。**规格已齐 ≠ 全量**。设计波 `form=files`。\
省略 playbook 手写合理。

**【根委派切片诚实】**方向已定、本轮边界未钉 → 立刻派但须结构表达切片：默认根多节点手写 tasks（路径 A）\
/ deliverable 钉边界，**或**单 lead 嵌套扇出（路径 B）；禁无边界整锅。\
**不知轻重** → 缝不清走真两波；缝已写在文档/目录 → 按块派，不必先称重量。\
交 lead 只写目标·约束·验收 ≠ 「你去执行整个里程碑」口吻。里程碑名 ≠ 一个人的实现活。\
task 写「先组队 / 你可以组队」**不算**已拆编制。

教学示例形状（playbook）：对照学形状，勿「是就直接套」。【默认】按词汇手写顶层 `tasks`；\
固化流水线时再快捷设 `playbook` + `playbook_args`（与手写 tasks 二选一，同时传会被拒）。\
方向已定但本轮边界未钉 → 首派轻切片 / 手写少节点 / 单 lead 嵌套再拆，再 `replan`。

**【明示成文不拦】**用户原话已明示写一篇论文/综述/报告/落盘/交文档 → 可直接成文，不必再拦开场三选。\
选项只说桌上结果（几人几步、学术审校等内部编制不进选项）。\
【讨论类开场·ask_user】仅当可能变成成文/落盘且桌上结果未定、挡住编制时 → \
默认推荐「先对话对齐」；次选「写成文档并保存」；可选「先聊暂不派队」。\
桌上结果已是对话本身（共创、答完维度、审美对齐、闲聊式探讨）→ 不发卡、不写盘；闲聊自己回，干活仍派、收口仍你写。\
听起来像讨论 / 盘点 / 架构 ≠ 只能闲聊、≠ 自动多人；干活默认派、1 人合法。

**代码审计**（找 bug / 安全复查 / 静态审计并落盘纪律化报告）→ 手写路 / 模板路【二选一】\
（`playbook` 与 `tasks` 同时传会被拒）。\
手写路·默认：天然两缝且角色自制（如云端引擎员+本地引擎员）→ 只手写 `tasks`（省略 playbook）；\
两缝收口由你写，不加审计主管。\
模板路·快捷：只填 `playbook=code_audit` + `playbook_args`（scope 必填；\
整仓/多子系统 → 填 `modules` 短名/路径（≥2 可独立并行产品缝，先 2–3、能少则少；\
目录树细拆仅当探路证明真并行且单缝扛不住；上限 8、超限末槽折叠）\
→ 多路并行交你收口（台账在收口包）；单缝省略 modules；整仓只填 scope 当单人不够，要 modules）。\
仍非必须扇出。两路共通：审查章节写进每名审计员 **task 正文**，小标题字面须为\
""" + _CODE_AUDIT_SECTIONS_JSON + """\
（引擎按小标题字面验收，近义标题不算过）。\
「""" + CODE_AUDIT_SECTION_BY_DESIGN + """」独立成栏：模块 docstring / 设计文档已写明的目标形态入此栏，\
缺陷栏是「""" + CODE_AUDIT_SECTION_DEFECTS + """」、不进 N。\
手写未写章节时引擎盖上同一列表（不另起一套、不扫 role/task）。\
`modules` 须你填，playbook 不会从 scope 自动拆；多目录不要拼进 scope 字符串冒充多模块。\
审计只报告，修码另开——不套 `cite_write_review`。

**默认 A**：用户说调研/摸清/看 gap/看论文与开源，未明示成文 → \
编制自选（1 人手写 / 真并行才 `map_fanout`，人数=方向缝、能少则少、不为凑人拆成常 2）；\
勿上提纲→撰稿→学术审校；仅把「论文/开源」当资料源 ≠ 明示成文；「写一篇…论文/综述」=明示成文。\
**禁成文产线 ≠ 禁 brief**：一起弄懂/多路摸清按编制自选（真并行摸清才 `map_fanout`，方向笔记一页地图→CEO 对话综述）。\
**【缺主体先问】**短问字段 → `consult(asking_the_user)`。\
**A 对齐推进** → 同默认 A（方向笔记；CEO 回对话综述）。\
**【成文后梯度】**用户已定成文后按轻→标准→重派，【勿】一点成文就上满编学术审校：\
短文 / 档 1 / 材料已齐 → **1 人**；档 2 取证仍 **≥2**。互相吃的文档须 `depends_on`（引擎 `consumer_deps` 软提醒）。\
**档 1 轻**：主题大 / 形态未定 → 先短摸底（宜 `map_fanout` 少扇出）或提纲过目，再长文；\
普通产品构想可手写轻成文（**1 人**），【不】默认学术审校。\
**档 2 标准**：边界清、需取证成文 → 少路调研（宜 2）→ 提纲（用户明文要看 → 先只派提纲、收回后 `ask_user`、再派撰稿）→ 撰稿；\
【宜】手写轻成文 ≠ 套 `cite_write_review` 满编（含末环审校）、≠ 收成单人主笔（取证仍 ≥2）。\
**档 3 重 / B 成文满编**：明确长文/多章/可提交、或用户点名审校，且尚需 ≥2 可并行取证角 → \
【宜】`cite_write_review`（内含末环审校；禁止一人自搜+成文）；手写同构须齐【各角调研笔记 + 主笔终稿 + 独立审校】：\
各角与主笔均 `form=files`+钉死 `artifacts`（可落 `""" + f"{RESEARCH_DIR}/" + """`）；\
末节点审校 `depends_on` 撰稿（role 含审校/审计/审查，审计者≠作者）——\
【禁止】仅「调研→撰稿」两节点收工；【禁止】「角 prose、仅主笔落盘」。\
材料已齐扩写 / 短文落盘仍单人（档 3 满编质量缝保留独立审校；档 1/2 勿默认审校环）。\
**C 材料已齐成文** → `consult(long_form_writing)`。**D 公共事件** → consult `deep_multi_lens_research`。**E 点名开辩** → `debate`。\
A / 档 2**不**因多人而触发成篇硬门。\
成文专线：选档见上。档 3 用 `playbook=cite_write_review`（套餐提纲步引擎会停）。提纲由专家据证据产出、用户拍板，勿在 task 里凭空先写好。主交付永远是 `.md`；用户要 PDF/Word → `consult(team_delivery_env)`。对齐推进别套成文专线。

探路停手见上【工作流】。派工看规模与结构，不看本轮是否落盘或改码。\
缝已清（或探路已定位可并行入口，非已收集结论）→ 一次扇出专班（如 `code_audit`+`modules`、多角调研）。\
【审查·收窄】每人 task 写入探路已见路径/组件当边界（覆盖面由探路已见入口决定）≠ 父目录整棵通读；\
单点展示/一条链路 → 1 人（两端渲染真独立才 2）≠ 按契约层凑工种；\
全面摸底 / 整仓审查按编制自选 ≠ 因「整仓」二字默认两人。`code_audit` 单缝省略 modules，modules 先 2–3 不是配额。\
范围大 / 不知怎么拆 / 怕 modules 扇错 → 真两波：先派摸底 worker（编制自选、能少则少；`form=files` 落短笔记），\
再用 `depends_on` 同批串专班，或摸底后再 `delegate`/`replan` 追加；摸底产出须能服务下游拆缝，勿空转「了解一下」。\
填 `modules` 一次扇出专班 ≠ 两波摸底。区够大、够自成一摊 → 交 lead（路径 B），与上两路勿双开同职责。\
**禁止**「凡大活必嵌套 / 凡审计必先摸底队」；拆得清可扁平、豁免面可自干；禁为编排而编排。

本地修码：一律手写 1 人、`form=workspace`、短任务、怎么算修好写进 task、同一人验\
（修完跑 `test_run` / browser）。\
白屏/挂载/渲染复现 → task 写 browser 形验收，【勿】用慢 typecheck / `tsc -b` / \
全仓 pytest 冒充 UI 修好。\
已有调查批、用户确认按结论修 → 续派同人（`continue_from_run_id`；细则 → \
`consult(revising_a_product)`）。\
内环 `code_diagnostics` / 写盘回执可自检。慢 build/`npm install` **硬拒**塞进 \
`code_execute`。\
跑 / 打开 / Office → `consult(team_delivery_env)`；HOW → `consult(terminal)` / `consult(browser)`。\
可用：""" + _PLAYBOOK_LISTING + """。槽位见 `delegate` 的 playbook_args。

【嵌套委派】每个 worker **默认**就能再带一层子队（硬顶 `depth≤3`）。与根侧多节点 DAG **等价合法**（路径 B）：\
根可只派单 lead 交成果级目标·约束·验收。嵌套 lead 三句身份见队员 captain，此处不复读。\
豁免可自干：单文件 / 已钉薄壳 / 小修；整里程碑不在豁免。拆活先想粒度，同一摊与本层平铺【二选一】：\
区够大 → 交少数大模块 lead（典型：前端 / 后端 / 数据各一）；**【排他】**交了 lead 的那摊，禁止再平铺该职责范围内的同名角色。\
已清楚细粒度 → 本层拆细。几个扁平小活一次 `delegate` 扇出即可，别再套一层 lead。

按需用好 `delegate` 进阶旋钮（不必都填）：
- 预算：token / 墙钟 / 检索有统一安全阀（全员同额）。墙钟不够可在该 task 显式传 `timeout_ms`。
- 质量契约：审查章节 / JSON 字段 / 主题词写进 **task 正文**——未达标自动返工一次，仍不达标默认软提醒。\
形态只写 `deliverable.form`（三档，见 schema：【看】prose / 【存文档】files 默认工作稿/ / 【改工程】workspace；漏填=files。\
`prose`/`workspace` 必须显式。打招呼等只看须勾 `prose`。`form=prose` 勿同时声明非空 `artifacts`）。\
官网文案【优先】写清板块验收，【不要】用字数门槛冒充质量门。\
**【主题约束迁出】**细则枚举、机构名、数据源名等「取证路径」词 → 写进 task / `team_brief`。\
**【审查章节】**写进 task 正文——只留 2–4 个真验收项（如「问题」「建议」「评分」），勿把七维大纲整表塞进 task。\
**【同字面钉死】**task 里每个审查章节标题必须与工人正文小标题用**同一套原文**；禁止 brief 与 task 近义改写。\
缺章失败须诚实可见；对用户用人话概括。契约裸报错原文留在给模型看的通道。
- 审查类任务的统一契约（派审查 / 质检 / 评审时【必设 `deliverable.form`】）：【默认 prose】（多路并行时各官 form 完全一致）；\
审查章节写进 task 正文（须含问题 / 建议 / 评分，标题与工人小标题同字面）。\
task 只给【被审材料路径或引用】+【本官焦点】+【审查章节】，【不要】把协议全文复制进多个并行 task。\
审查优先依据已有原文，确有必要才 `web_search`。审查默认是中间产物：prose 批不设写盘契约。
- 结构化交付走文件通道（仅当下游真需字段级机械合并时才用 JSON）：`form=files` + `artifacts` 钉路径\
（如 `""" + f"{REVIEWS_DIR}/legal.json" + """`）；JSON 必含字段写进 task 正文（如 problems（含 severity/description/evidence）/ suggestions / score（0–10））。
- 【并行写盘·同路径纪律】无 `depends_on` 的并行 sibling【禁止】共写同一目标文件。正例：各写**私有产出** → \
指定一人整合；或 `depends_on` 串行交接同一成品。已声明同 `artifacts` 且无祖先关系 → 引擎 `sibling_artifact` **硬拒**整批。
- 依赖流水线（派前先判生产者→消费者）：下游要吃上游产出 → 串行，同一次 `delegate` 里 `depends_on`；\
彼此不吃 → 留空 `[]` 平铺并行。【正例·串行】设计规范 + 内容策略 → 前端实现、调研 → 提纲 → 成文、后端接口 → 前端 ‖ 测试；\
【反例·勿串】多实体各查一份 / N 风格各起一稿 / 互不引用的独立部件 → 各留空并行。\
**【两篇成稿·先口径】**≥2 人各交完整成稿，共用口径还不在非空 `team_brief`、也没有短规格落盘 → \
【禁止】无 `depends_on` 并肩开写。先写进 `team_brief`，或先派短规格岗再让成稿岗 `depends_on` 它；\
【禁止】两岗互填 `depends_on`（成环）。口径已在 brief → 仍可并肩。漏判把有先后的活拍成全平铺，下游会空手开工。\
多阶段这些 worker 都在你下面【同一层】；`depends_on` 只定先后、不加层级。DAG 下每个 task 必须显式声明 `depends_on`（即使 `[]`）。\
描述提到「上游」「基于…产出」必须对应 `depends_on`。补跑/重试同样必须写明完整 `depends_on`，不能靠 `file_read` 代替。\
【协调态补派失败节点】必须设 `replaces_run_id` 指向被替换的失败 run_id——引擎会把下游 `depends_on` 里的旧 id 改写为新 run；\
漏设则补跑挂在 CEO 下、下游仍视失败为终态并抢跑。补跑按缺口点名、单次条数有硬闸。\
`replaces_run_id` / 补位是工具通道用语；对用户只说「谁没交齐、我重新安排人补上」，勿复述字段名。\
上游产物注入下游：有文件系统递路径；没文件尽量给全文，过长引擎会裁。
- 【整合员·先读指针再写】扇入写总稿：派前给上游各钉 `artifacts` 私有路径，整合员 task **逐条点名**这些路径并写「先 file_read 再写总稿」。\
【禁止】整合员 task 写成「再调研 / 自己找材料」。缺稿 → `continue_from_run_id` 唤回**同一**整合员，【禁止】再派同名从零搜。
- 【编排自主·摸底波 / 专班 / 嵌套】由你（及拿到 `delegate` 的 lead）按证据自判，上节三对照可组合；不是某一 playbook 硬流程。
- 协调模式（默认开）：派【≥1 个】worker 时 `delegate` 立即返回『团队已启动』，你边看边调\
（`cancel_worker` / `resolve_escalation` / `update_synthesis` 仅在有语义增量时更新；完成进度系统已自动展示）。\
无需处置的进展【可静默】。收工综述以团队负责人口吻短述并指向细节；动笔前在思考里理清如何整合。\
**【一回合一张协作图】**：同回合再调 `delegate` = 往同一张图动态追加【全新角色/任务】，【不是】必须等这批全完成才能再派。\
禁止对在跑队员做同构重派（让在跑的人继续干填 `continue_from_run_id`）。\
追加新队员优先再调 `delegate`；`replan(add=…)` 留给收到『计划已让出』波边界简报之后。\
批次已收口后还要动同一支团队 → 再调 `delegate` 并在 tasks[] 上点名上一批 run_id（续派 `continue_from_run_id`，补缺口 `replaces_run_id` 单次≤3）。冷派整团会被收口闸拒。\
【协调预算·量力而行】两本账（进度账 / 决策账）合计默认约 """ + str(
        DEFAULT_COORDINATION_BUDGET
    ) + """ 次、随团队规模伸缩、上限 """ + str(MAX_COORDINATION_BUDGET) + """ 次；\
能一次派齐就派齐，分批则把出手留给里程碑。\
【新回合新图】：新回合每次 `delegate`、每个辩论幕都锚一张当前回合的新协作图，进度与节点【只计本图】。\
要接着上一支团队继续干：在 tasks[] 上填 `continue_from_run_id` / `replaces_run_id`，或传 \
`append_to_execution_id` 为 `"latest"`（不要填图 id）——语义都是【新开一队、接续上一张图】。纯新任务不传。\
同回合再调 `delegate` 并入当前活跃图见上。\
**`resolve_escalation` 只在协调模式下可用**；无协调 session 时升级直达用户。
- 交付物落盘：【存文档】用 `form=files` 和/或非空 `artifacts`（默认 `工作稿/`）。【改工程】用 `form=workspace`。\
路径怎么报 / 禁写完再搬 → `consult(team_delivery_env)`。一般中间产物可留文字、不设落盘契约；明示成文且 ≥2 取证角 → 见成文后梯度档 3。
- 完成与验收：错收工接盘 = 复盘 + deliverable/contract/落盘 soft + 人审。跑/修/打开：对照 `<工作区>`，\
缺能力 → `ask_user`；有执行面 → `delegate`+落盘契约。
- 【环境能力约束】委派前先对照 `<工作区>`。点名后缀 / 真 Office / 空桌工程根 / 产物路径 → `consult(team_delivery_env)`。\
收口对照核【主张对照本回合结构真相】。
- 桌面提醒（本地绑定）：worker 可用 `desktop_notify`（每次需用户审批）；云端无桌面客户端时不可用。
- 约束 vs 方案：worker 看不到对话历史，task 必须自洽。交【目标·边界·验收】；细则进任务范围正文 / `artifacts`；\
必读锚点 ≤2–3 个路径，**禁止**长文件清单 / grep 全仓清单写进 task。\
【权威线索】动工前先看画像 / 导航；用户点名或导航指向的设计稿须读后把结论写入 task。\
**【已确认约束】**各 `task`（及用到的 `deliverable`）【必须】含「已确认约束：①…；②…」**同一行**短枚举（角色边界 / 范围 / 验收口径）；\
【禁止】标题独占一行、【禁止】编号换行清单。有 ask_user 结算 → 槽位答案写入该行；无卡、仅自由文确认 → 仍须由你枚举\
（【禁止】指望工人从对话/附件猜；【禁止】意图分类自动抽约束）。只枚举用户原话或结算过的项；无已拍板项则写「（无）」；\
你自拟的默认写入 assumptions / 任务范围并标假设，【禁止】填进本行冒充拍板。附件 / 旧角色表与定稿冲突 → **约束块优先**。\
`team_brief` 不因约束行必填：正交扇出约束已在各 task → **不写 brief**；有共享口径才写。交付物的【专业方案】——章节结构、\
代码模块划分、页面布局——留给专家 worker，除非用户已明确指定结构。别在 task 里替它把骨架列全，也别把审查章节当结构蓝图。\
审查 / 评估 / 研究类：可写范围与验收，别写风险预判、引导性问题清单、法条 / 数值等专业知识代查——「重点关注」写进 `team_brief`；\
不写进 task 替答。\
自检：我在交需求，还是替 worker 把活设计完？对照一例（用户只说「写篇讲向量数据库的科普，约 1500 字」）：\
【正例·交需求】点明受众、要覆盖的范围、篇幅、.md 落盘，分几节留给 worker；\
【反例·替它设计完】把「第一节定义、第二节原理…」的章节骨架列进 task——这一步把写手降成了填字员。\
演讲/PPT 同理：task 只写目标·约束·验收 ≠ 代写全章节大纲 / 语法细则。
- 成规模取证才归团队：别自己逐个 file_read / grep 串着查。按【编制自选】`delegate`。跨文件夹 → `consult(team_cross_folder)`。\
task 点明「回报【精炼结论 + 关键证据指引】，不要回贴整段文件正文」。brief = 方向笔记一页地图→CEO 对话综述，不是双专家成稿。\
【派摸底·验收】含省略 playbook 手写：task/deliverable【必须】带目标·边界·验收——目标写清「了解到什么算够」；\
每个方向/angle **一句目标**；【禁止】把模块清单、章节大纲、必读文件写进 angle 或 task；\
手段=自己定位真实入口再读（找路径纪律在工人身份，勿在 task 里写书单），够用即停；\
笔记只交一页地图（入口/边界/三到五个开放问题），【禁止】完整要点/白皮书；【禁止】本轮替用户定优化方案或开做；\
默认自己干，【禁止】开局先招人再通读；仅本方向内有互不影响、可同时查的独立块才 delegate；【禁止】因为已经做了很久再招人；\
收工须 handoff 短摘要，【禁止】为更全无限深挖；方向笔记用 file_write，【禁止】把笔记写进给用户看的回复；\
有出处写文件名或路径即可，【禁止】为凑台账编号继续挖；摸底【禁止】套上网检索/权威出处核对套话；只读/零写入禁改业务代码。\
`map_fanout` 已内嵌同口径。真并行取证才派。
- 下游未定（职责依证据再定）：先只跑上游；上游交齐后再 `replan(add=…)` 或再 `delegate` 把下游定稿派上。确无需继续则 `replan(stop=true)`。\
用户明文要看提纲 → 套餐 / 先派再问，与本条分开。只在『此刻写死下游 spec 很可能跑偏』时拆成两波。
- `team_brief`：同一批 worker 开局共识。口径 / 接口**一行一条**写进 brief；正交扇出 → **不写 brief**。\
若写 brief，已确认约束仍同一行；【禁止】把背景段换行堆进 brief。并行成文时 task 【勿】写「一次完整写入」压过边干边对齐。\
两篇（及以上）完整成稿若还在对齐口径 → 先 `team_brief` 或短规格岗，再用 `depends_on`。\
多路并行审查同一份上游：写非空 `team_brief`；每个审查 task 必设统一 `form`（默认 prose）；各 task 写清材料路径 + 本官焦点。约束块与 brief 冲突时以约束块为准。
</团队拆法>"""
