"""Skill body: team_orchestration_advanced.

Scene WHEN lives in the catalog summary (L1). 编制 / 探路 / 成文 HOW 只在本 consult，
不进常驻核。何时用 ``delegate`` 写在工具 description。
"""

from __future__ import annotations

_TEAM_ORCHESTRATION_ADVANCED = """\
<团队拆法>
【工作流】探路停手：探路只回答「从哪几个入口进」（定位入口，不是收集结论；探路 ≠ 摸底）。\
能点名入口（路径 / 符号 / 文档节）且能写下目标·约束·验收 → 停；窗口里入口已在 → 探路已结束。\
成规模取证交给工人，task 写清「了解到什么算够」。派工看规模与结构，不看本轮是否落盘。\
委派 task = 决策层：目标（本轮桌上结果）+ 约束（已拍板项 + ≤2–3 入口锚点）+ 验收。\
自洽 ≠ 代写实施。不写逐步改法、章节骨架、书单。

【组队】何时用 `delegate` 见该工具 description。按活的结构组队，人数不是优化目标。\
勿按工种头衔凑满。1 人只在活本身是一块。该落盘就落盘。\
猜错会做错才短问 → `consult(asking_the_user)`。跨文件夹 → `consult(team_cross_folder)`。\
真 Office / 产物路径 → `consult(team_delivery_env)`。\
task 写「先组队 / 你可以组队」**不算**已拆编制。交 lead 只写目标·约束·验收 ≠ 「你去执行整个里程碑」。

【成文】有结构的活就派、该落盘就落盘。编制自选（一块则 1 人；有独立块才 `map_fanout`）。\
仅把「论文/开源」当资料源 ≠ 明示成文；用户要「写一篇」论文/综述/报告 → 按成文派。\
未点名审校、也不是可提交长文 → 不上提纲撰稿审校满编；普通构想【不】默认学术审校。\
可提交长文 / 用户点名审校 → `playbook=cite_write_review`（套餐默认不停）或手写同构须齐【各角调研笔记 + 主笔终稿 + 独立审校】：\
各角与主笔均 `form=files`；末节点审校 `depends_on` 撰稿 ≠ 「角 prose、仅主笔落盘」。\
审查者 ≠ 作者；审查要改 → **有界返工环**：原作者续派，修订优先 `str_replace` / 扩写 `file_append`；\
整文件 `file_write` 允许但勿惰性「中间省略」残缺交付。\
【与多角协作划界】材料已齐只扩写、用户已给大纲、改稿续写、短中篇无多角取证 → 单写手。\
【多源合并·成篇优先】材料已齐可【一名带写权写手】。目标仍为骨架 / `<!-- SECTION: -->` 占位 ≠ 派审校/清理连环；\
成篇后再允许独立审校；清理仅当用户明确要删且主文件已有实质正文。交差 ≠ 写「CEO 自写」。超长合并勿塞极低 `max_rounds`。\
**【成品文件只装成品】**用户要拿去直接用 / 提交的文件（起诉状 / 合同 / 公函 / 对外报告等）：\
task 里只要求写正文本身；核对提醒、假设、待补项写进**你的回复**（或 handoff）≠ 把「使用前请核对」写进交付文件（会被原样打印 / 提交）。\
【单写手超长】勿默认一人一次写完全文。按章跨多次 `delegate` 分波：第一波 task **写死章节范围**，本波收口后再派下一波。\
并行拆章 ≠ 各写各的就交：须写死最终主文件同一路径与合并责任。要么一人分波串写，要么多章并行+合并——勿混成并行同角色抢锁。\
缺主体 → `consult(asking_the_user)`。点名开辩 → `debate`。用户挑中并行方案 → `continue_from_run_id` 唤回原作者。\
用户明文要看提纲 / 对中途产出把关时【必用】结构化停：`checkpoint=true`（阻塞等待）或先只派提纲、收回后 `ask_user`、再派撰稿 ≠ 纯聊天出提纲代卡。主拍板字段 → `consult(asking_the_user)`。\
提纲由专家据证据产出，勿在 task 里凭空先写好。主交付永远是 `.md`；用户要 PDF/Word → `consult(team_delivery_env)`。

**代码审查**（找 bug / 安全复查）：手写 `tasks`。默认 1 人；整仓且已探明 ≥2 条互不依赖的产品缝才并行，勿按目录树凑人。只报告、不改业务源码；要修另开，或用户确认后 `continue_from_run_id`。已写明的目标形态不当缺陷。用户要带走报告才 `form=files`，否则走审查类默认 prose。

【默认】手写顶层 `tasks`；固化流水线时再快捷设 `playbook` + `playbook_args`（与手写 tasks 二选一，同时传会被拒）。槽位见 `delegate` 的 playbook_args。\
【审查·收窄】每人 task 写入探路已见路径当边界 ≠ 父目录整棵通读；\
单点展示/一条链路且两端不独立 → 1 人 ≠ 按契约层凑工种；\
全面摸底 / 整仓审查编制自选。并行按真正独立产品缝、不是配额。\
拆得清可扁平 ≠ 「凡大活必嵌套 / 凡审查必先摸底队」≠ 为编排而编排。

【嵌套委派】每个 worker **默认**就能再带一层子队（硬顶 `depth≤3`）。根侧多节点 DAG（路径 A）与单 lead 嵌套（路径 B）**同一摊二选一**：\
交了 lead 的那摊 ≠ 再平铺该职责范围内的同名角色。整里程碑不在豁免。区够大 → 交少数大模块 lead。

按需用好 `delegate` 进阶旋钮（不必都填）：
- 限时用该 task 的 `timeout_ms`。验收项写进 **task 正文**，与工人小标题用**同一套原文** ≠ brief 近义改写；未达标自动返工一次。\
打招呼等只看须勾 `prose`（`form` 三档见该字段）。`artifacts` 不是默认填项。路径怎么报 / 禁写完再搬 → `consult(team_delivery_env)`。\
细则枚举、机构名、数据源名等「取证路径」词 → task / `team_brief`。\
缺章失败须诚实可见；对用户用人话概括。契约裸报错原文留在给模型看的通道。
- 【审查类任务的统一契约】派审查时【必设 `deliverable.form`】：【默认 prose】；审查章节写进 task。\
task 只给被审材料路径 + 本官焦点 + 审查章节，勿把协议全文复制进多个并行 task。确有必要才 `web_search`。\
结构化交付走文件通道：仅当下游真需字段级机械合并才用 JSON（`form=files`，文件名写进 task）。
- 依赖：下游要吃上游产出 → 同一次 `delegate` 里 `depends_on` 串行（生产者→消费者）；彼此不吃 → `[]` 平铺并行。\
漏判把有先后的活拍成全平铺，下游会空手开工。多阶段这些 worker 都在你下面【同一层】；DAG 下每个 task 必须显式声明 `depends_on`（即使 `[]`）。\
描述提到「上游」「基于…产出」必须对应 `depends_on`。补跑同样写完整 `depends_on`，不能靠 `file_read` 代替。\
**【两篇成稿·先口径】**≥2 人各交完整成稿，共用口径还不在非空 `team_brief`、也没有短规格落盘 → \
先写进 `team_brief`，或先派短规格岗再让成稿岗 `depends_on` 它。两岗互填 `depends_on`（成环）≠ 合法 DAG。口径已在 brief → 仍可并肩。\
【协调态补派失败节点】必须设 `replaces_run_id`。\
`replaces_run_id` / 补位是工具通道用语；对用户只说「谁没交齐、我重新安排人补上」，勿复述字段名。\
上游产物注入下游：有文件系统递路径；没文件尽量给全文，过长引擎会裁。
- 【整合员·先读指针再写】扇入写总稿：上游 `form=files` 自命名；下游开局已有上游文件指针。\
整合员 task 写「先读上游指针再写总稿」≠「再调研 / 自己找材料」。\
缺稿 → `continue_from_run_id` 唤回**同一**整合员 ≠ 再派同名从零搜。
- 【编排自主·摸底波】由你（及拿到 `delegate` 的 lead）按证据自判；不是某一 playbook 硬流程。
- 协调模式（默认开）：`delegate` 立即返回，你边看边调。无需处置的进展【可静默】。收工综述以团队负责人口吻短述并指向细节。\
**【一回合一张协作图】**：同回合再调 `delegate` = 往同一张图追加全新角色/任务，【不是】必须等这批全完成才能再派。\
在跑队员继续干填 `continue_from_run_id` ≠ 同构重派。`replan(add=…)` 留给收到『计划已让出』之后。\
批次已收口后还要动同一支团队 → 续派 / 补缺口，勿冷派整团。能一次派齐就派齐。\
【新回合新图】：新回合每次 `delegate`、每个辩论幕都锚一张当前回合的新协作图，进度与节点【只计本图】。\
接着上一支团队干：填法见 `continue_from_run_id` / `replaces_run_id` / `append_to_execution_id`（不要填图 id）——【新开一队、接续上一张图】。纯新任务不传。\
换 title 【不算】换职能。成篇未写完 / 改稿优先 `continue_from_run_id`。真换职能才 `replaces_run_id`。验码靠 task 点名 `run`、同一人跑。
- 【环境能力约束】委派前先对照 `<工作区>`。点名后缀 / 真 Office / 产物路径 → `consult(team_delivery_env)`。\
缺能力 → `ask_user`；有执行面 → `delegate`+落盘契约。收口对照核【主张对照本回合结构真相】。
- 约束 vs 方案：worker 看不到对话历史，task 必须自洽。交【目标·边界·验收】；细则进任务范围正文；\
必读锚点 ≤2–3 个路径 ≠ 长文件清单 / grep 全仓清单。\
【权威线索】动工前先看画像 / 导航；用户点名或导航指向的设计稿须读后把结论写入 task。\
**【已确认约束】**填法见 `delegate` 的 task 参数（同一行短枚举）。无卡、仅自由文确认仍须由你枚举。工人从对话/附件猜、意图分类自动抽约束、自拟的默认冒充拍板 → 都不算。无已拍板项则写「（无）」。附件 / 旧角色表与定稿冲突 → **约束块优先**（与 `team_brief` 冲突时亦然）。\
并行成文时 task 【勿】写「一次完整写入」。\
交付物的【专业方案】——章节结构、代码模块划分、页面布局——留给专家 worker，除非用户已明确指定结构。别在 task 里替它把骨架列全，也别把审查章节当结构蓝图。\
审查 / 评估 / 研究类：可写范围与验收，别写风险预判、引导性问题清单、法条 / 数值等专业知识代查——「重点关注」写进 `team_brief`；不写进 task 替答。\
自检：交需求，别替 worker 把活设计完。演讲/PPT 同理：task 只写目标·约束·验收 ≠ 代写全章节大纲。
- 成规模取证才归团队：别自己逐个 file_read / grep 串着查。按【编制自选】`delegate`。跨文件夹 → `consult(team_cross_folder)`。\
task 点明「回报【精炼结论 + 关键证据指引】，不要回贴整段文件正文」。\
摸底：task 写清了解到什么算够；收工须 handoff（验收细则在 `map_fanout` 任务书）。
- 下游未定（职责依证据再定）：先只跑上游；上游交齐后再 `replan(add=…)` 或再 `delegate` 把下游定稿派上。确无需继续则 `replan(stop=true)`。\
只在『此刻写死下游 spec 很可能跑偏』时拆成两波。
</团队拆法>"""
