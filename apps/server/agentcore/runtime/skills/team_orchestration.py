"""Skill body: team_orchestration_advanced.

Scene WHEN lives in the catalog summary (L1). Must-not-consult on common paths
stays in CEO core ④, not a shared intensity string.
"""

from __future__ import annotations

from agentcore.runtime.coordination.session import (
    DEFAULT_COORDINATION_BUDGET,
    MAX_COORDINATION_BUDGET,
)
from agentcore.runtime.runs.playbooks import available_playbooks
from agentcore.runtime.runs.playbooks.audit import (
    CODE_AUDIT_REQUIRED_SECTIONS,
    CODE_AUDIT_SECTION_BY_DESIGN,
    CODE_AUDIT_SECTION_DEFECTS,
)
from agentcore.workspace.stage_dirs import RESEARCH_DIR, REVIEWS_DIR

# playbook 降级为形状词汇教学示例（协作优先重设计阶段 2）：listing 仍嵌进 skill，口径改为对照学形状。
_PLAYBOOK_LISTING = available_playbooks()
_CODE_AUDIT_SECTIONS_JSON = (
    "[" + ", ".join(f'"{s}"' for s in CODE_AUDIT_REQUIRED_SECTIONS) + "]"
)

_TEAM_ORCHESTRATION_ADVANCED = """\
<team_orchestration_advanced>
改产物 / 成规模取证该派就派（桌上结果已定之后）。先想形状再拆任务——教的是【词汇 + 组合】，不是成品模板。\
自检：换个主题，形状还一模一样吗？还一样就错了。\
「调研+写码+点评+合成一篇」这类跨域合成流水线 → 常见 1～2 人，勿默认每人一种专长；\
档 3 成篇满编的【产出→独立审校】是质量缝、不算凑工种；普通构想 / 档 1–2【不】默认学术审校。\
「先设计再实现」小CRUD/骨架 → 默认 **真两段** / **1 人两段**（结构拆开：wave1 设计/API +\
`form=files` 交回后再实现；或同批 ≥2 tasks + `depends_on` / \
同人 `continue_from_run_id`）；**【假两段·禁】**同一 task 写「先交设计再实现」。\
设计很重或要点名评审再升 2 人串。\
前端 UI / 壳层强耦合改造（同系统多面：状态条、通知、空状态面板等）→ 同默认真两段，\
或 wave1 只交设计 / API 契约（`form=files`），实现波再落盘 + `playbook_args.verify` / task 写清怎么验；\
**禁止**第一棒塞「设计 + 双子系统 + 壳层 + build」。\
**桌面壳 / 多进程绿场**：省略 playbook 手写合理，禁首 grant「设计 + 主进程/渲染/核心运行时 + 可跑闭环」一口吞；\
先 DESIGN 或更瘦壳，闭环另棒（或单 lead 嵌套再拆）。\
**多屏 UI / 单文件大原型** → 默认 MVP 或同上真两段 / wave1=`form=files`；\
**禁止**首 grant「完整可玩 N 屏」（桌上档 / `playbook_args` 等结构槽已点「一次做完」除外；禁扫长文）；\
**规格已齐 ≠ 全量**。\
单页一人做完；控制台别套营销皮；HTML 落盘即自动静态质检；勿误伤 light 小活 / 短文落盘。

**【根委派切片诚实】**方向已定、本轮边界未钉 → 立刻派但须结构表达切片：默认根多节点手写 tasks（路径 A） / deliverable 钉边界（固定流水线可快捷具名 playbook），\
**或**单 lead 嵌套扇出（路径 B）；禁无边界整锅。\
**不知轻重** → **禁止**猜「一人能扛整座成果」。缝不清 → 走下方②真两波，【禁止】自己连搜当摸底；\
缝已写在文档/目录 → 按块派，不必先称重量。\
交 lead 只写目标·约束·验收；**禁止**「你去执行整个里程碑」口吻。里程碑名 ≠ 一个人的实现活。

形状词汇（按任务结构选、可组合）：
- 并列对象分组：每对象一员（重档升 lead 内拆维度），尾挂横向汇总
- 角度扇出：N 角度并行调研 / 产出，汇入下游
- 证据驱动流水线：调研 → 结构定稿（主拍板）→ 产出
- 独立审查：审查者 ≠ 作者，产出结构化问题清单
- 有界返工环：审查发现 → 原作者带现场续派修订 → 复核，≤2 轮，到限交代缺口；\
修订按审校意见逐条用 `str_replace` 局部改（优先）、扩写用 `file_append`；\
整文件 `file_write` 覆盖允许但勿惰性省略中段（反例：「……（中间省略，已保留首尾）……」致残缺交付）
- 契约共享面：非空 team_brief 建墙播种口径 / 接口 / 约束，decision 便签广播
- 独立多透镜诊断：对既有材料 N 透镜并审 → 风险分级汇总 → 风险确认卡\
（`ask_user` 带 `card="risk_ack"`）让用户勾选要处理项 → 定向修订
- 部件一致性对账：并行部件起草 → 对账节点专查接缝 → 定向修订
- 对抗辩论：Moderator 驱动交锋；其他原型真冲突才开局部辩（勿默认冲突即辩）
- 发散挑选：N 风格并行 →（轻量互评）→ 方案挑选卡（`ask_user` 带 `card="proposal_pick"`）\
让用户挑一个 → 中选深化

组合：多对象+成篇 → 分组×流水线；构建+并行模块 → 契约共享面+独立验证；\
审查要改 → 接有界返工环；结论真冲突 → 局部辩论；跨域合成一篇 → 少派串起，勿按工种堆人。

【跨文件夹并行指挥】HOW → `consult(team_cross_folder)`。空桌工程根 / 真 Office / 产物路径 → `consult(team_delivery_env)`。

三档：默认中档。轻=保底（构建类轻档也要「实现+独立验证」双人）；\
重=任务规模大或用户点名才上。控税靠选档与按缝拆人，不靠默认单干、也不按工种凑满。

教学示例形状（playbook）：下列是词汇表的可实例化快捷套餐——对照学形状，勿「是就直接套」。\
【默认】按词汇手写顶层 `tasks`；固化流水线时再快捷设 `playbook` + `playbook_args`（与手写 tasks 二选一）。\
绿场软件【快捷套餐】具名 playbook（见 consult `build_app`）；手写可用，勿在此复读全文。\
建站用手写顶层 `tasks`；单页一人做完。\
方向已定但本轮边界未钉 ≠ 绿场 SPA 满档：首派轻切片 / 手写少节点 / 单 lead 嵌套再拆，再 `replan`；\
仅真 SPA / 用户明示完整可跑 / 点选模块流水线才满档 `build_app`（五阶段不可跳仅约束进入后）。\
**【明示成文不拦】**用户原话已明示写一篇论文/综述/报告/落盘/交文档 → 可直接成文，不必再拦开场三选。\
选项只说桌上结果，【禁止】写内部编制（几人几步、学术审校）。\
【讨论类开场·ask_user】仅当可能变成成文/落盘且桌上结果未定、挡住编制时 → \
默认推荐「先对话对齐」；次选「写成文档并保存」；可选「先聊暂不派队」。\
桌上结果已是对话本身（共创、答完维度、审美对齐、闲聊式探讨）→ 不发卡、不写盘；CEO 自己开口。\
听起来像讨论 / 盘点 / 架构 ≠ 只能闲聊，也 ≠ 必须开组、≠ 自动两路；编制自选。\
**代码审计**（找 bug / 安全复查 / 静态审计代码并落盘纪律化报告）→ 默认手写 / 快捷模板【二选一】，\
【禁止】`code_audit`（或其它具名 playbook）与 `tasks` 同时传：\
①【手写路·默认】天然两缝且角色自制（如云端引擎员+本地引擎员）→ 只手写 `tasks`\
（省略 playbook）；两缝【禁止】再加审计主管（收口由你写）；【禁止】再带 `code_audit`。\
②【模板路·快捷】只填 `playbook=code_audit` + `playbook_args`（scope 必填；\
整仓/多子系统 → 填 `modules` 短名/路径（≥2 可独立并行产品缝，先 2–3、能少则少；\
【禁止】按目录树默认拆到上限；目录细拆仅当探路证明真并行且单缝扛不住；\
上限 8、超限末槽折叠）\
→ 2 路并行无主管（你收口）；≥3 路才并行+主管速览；单缝省略 modules；【禁止】整仓只填 scope 当单人）；【禁止】再传 `tasks`。\
两路共通：审查章节写进每名审计员 **task 正文**，小标题字面须为\
""" + _CODE_AUDIT_SECTIONS_JSON + """\
（与模板路同字面；引擎按小标题验收；禁止近义改写）。\
「""" + CODE_AUDIT_SECTION_BY_DESIGN + """」独立成栏：模块 docstring / 设计文档已写明的目标形态入此栏，\
禁止标进「""" + CODE_AUDIT_SECTION_DEFECTS + """」、不进 N。\
手写未写章节时引擎盖上同一列表（不另起一套、不扫 role/task）。\
【禁止】指望 playbook 从 scope 自动拆；【禁止】把多目录拼进 scope 字符串冒充多模块；\
【禁止】套 `cite_write_review` 学术审校环；【禁止】与 `diagnose_fix_verify` 混用——审计只报告，修码另开。\
**默认 A**：用户说调研/摸清/看 gap/看论文与开源，**未**明示成文 → \
编制自选（1 人手写 / 真并行才 `map_fanout`，\
人数=方向缝、能少则少、【禁止】为凑人拆成常 2）；\
勿上提纲→撰稿→学术审校；\
仅把「论文/开源」当资料源 ≠ 明示成文；「写一篇…论文/综述」=明示成文。\
**禁成文产线 ≠ 禁 brief**：一起弄懂/多路摸清按编制自选（真并行摸清才 `map_fanout`，方向笔记一页地图→CEO 对话综述）。\
**【缺主体先问】**三路/多路调研未点名主体（产品/市场/事件/对象）→ 先 `ask_user`\
（题须预填 `default`）；用户 continue = 确认该 default，派工标「按确认默认」；\
无 default 不得 continue 派工；【禁止】静默自拟 topic/市场占位再派。\
**A 对齐推进**（一起弄懂 / 多路摸清 / 未明示成文）→ 同默认 A（方向笔记；CEO 回对话综述）。\
**【成文后梯度】**用户已定成文（原话明示或点选「写成文档」）后按轻→标准→重派，\
【勿】一点成文就上满编学术审校：\
短文 / 档 1 / 材料已齐 → **1 人**；档 2 取证仍 **≥2**。\
互相吃的文档须 `depends_on`（只提示词纪律；【禁止】升级成 `consumer_deps` 拒收闸）。\
**档 1 轻**：主题大 / 形态未定 → 先短摸底（宜 `map_fanout` 少扇出）或提纲过目，再长文；\
普通产品构想可手写轻成文（**1 人**），【不】默认学术审校。\
**档 2 标准**：边界清、需取证成文（含规格已齐的论文/综述）→ 少路调研（宜 2）→ 提纲（用户明文要看 → 先只派提纲、收回后 `ask_user`、再派撰稿）→ 撰稿；\
【宜】手写轻成文；【禁止】套 `cite_write_review` 满编（含末环审校）；【禁止】收成单人主笔（取证仍 ≥2）。\
**档 3 重 / B 成文满编**：明确长文/多章/可提交、或用户点名审校，且尚需 ≥2 可并行取证角 → \
【宜】`cite_write_review`（内含末环审校；禁止一人自搜+成文）；手写同构须齐\
【各角调研笔记 + 主笔终稿 + 独立审校】：\
各角与主笔均 `form=files`+钉死 `artifacts`（可落 `""" + f"{RESEARCH_DIR}/" + """` 或同构目录）；\
末节点审校 `depends_on` 撰稿（role 含审校/审计/审查，审计者≠作者）——\
【禁止】仅「调研→撰稿」两节点收工；【禁止】「角 prose、仅主笔落盘」。\
成稿两阶段引用走具名 `cite_write_review`（playbook 内含时序）。\
材料已齐扩写 / 短文落盘仍单人（档 3 满编质量缝保留独立审校；档 1/2 勿默认审校环）。\
**C 材料已齐成文** → 可单写手（见 `long_form_writing`）。**D 公共事件** → consult `deep_multi_lens_research`。**E 点名开辩** → `debate`。\
A / 档 2**不**因多人而触发成篇硬门。\
【禁止】开局自己连搜多轮做完整场再派——探路默认 0～1 轮只定位入口（脊柱见核【探路 ≠ 摸底】）。\
派工看规模与结构，不看本轮是否落盘或改码。\
本地修码：【无先验调查批】单文件/单符号一刀切 → 手写 1 人、`form=workspace`、短任务；\
有复现症状 / 多点 / 需验 → `diagnose_fix_verify`（单症状三波；`playbook_args` 必填 \
`problem` + `verify`）。白屏/挂载/渲染复现 → `verify=` 写 browser 形说明\
（如「打开 /app 白屏消失+snapshot 可见主内容」），【勿】默认全仓 tsc/pytest 冒充 UI 修好。\
【已有多角调查/审查批、用户确认按结论修】→ **禁止**再套 \
`diagnose_fix_verify` 冷开新三角色；手写 tasks + 对各调查 run 设 `continue_from_run_id`（可并行；\
可改 task 正文/title，换马甲≠换职能；队员坐本任务桌用相关工具面，不必再填 tools 收窄）。\
**禁止**把手写当修码默认、禁止触顶后再派马甲从零读。\
可用：""" + _PLAYBOOK_LISTING + """。槽位见 `delegate` 的 playbook_args。

按需用好 `delegate` 的进阶旋钮（不必都填）：

- 预算（统一 backstop）：worker 的 token 顶 / 墙钟 / 检索次数有统一安全阀（全员同额，\
不可按 task 配置检索额度）。墙钟若仍不够，可在该 task 里显式传 `timeout_ms`（毫秒）覆盖。
- 质量契约：对产出有验收要求时，【审查章节 / JSON 字段 / 主题词】写进 **task 正文**——\
未达标会带着具体差距自动返工一次；返工后仍不达标默认仅附质检提醒（软）。\
形态只写 `deliverable.form`（三档）与可选 `artifacts`（钉路径）。\
官网 / 营销文案类【优先】在 task 里写清板块验收，【不要】用字数门槛冒充质量门；\
主题词 / 结论要素写进 task 或 `team_brief`，勿堆机构名硬门槛。\
**【主题约束迁出】**细则枚举清单、机构名、数据源名、报告标题等「取证路径」词 → 写进 task / \
`team_brief`（【禁止】另造启发式闸或软提醒清单字段）。\
**【审查章节】**写进 task 正文——只留 2–4 个真验收项（如「问题」「建议」「评分」），\
结构细节留给 worker；勿把七维大纲整表塞进 task 当蓝图（与下条「约束 vs 方案」同旨）。\
**【同字面钉死】**task 里每个审查章节标题必须与工人正文小标题用**同一套原文**\
（引擎按小标题字面验收）；【禁止】brief 写「结论要点」而 task 写「结论」、或工人改写成近义标题。\
【禁止】为「少吓用户」而瞒报失败/缺口——缺章失败须诚实可见；对用户用人话概括（见 CEO 核心【面向用户·大白话】），\
勿改口称已过关。契约裸报错原文留在给模型看的通道（过程线/简报），靠上游钉字面少空转。
- 审查类任务的统一契约（派【审查 / 质检 / 评审】worker 时【必设 `deliverable.form`】）：无论并行扇出\
还是 `depends_on` 链下游，每个审查官都必须带 `form` 锁定统一形态——否则各审查官\
各说各的、打分维度各异，你收工时难对齐。**【默认 prose】**（多路并行时各官 form 完全一致，\
只换 role 与审查侧重）；审查章节写进 task 正文（须含问题 / 建议 / 评分，标题与工人小标题同字面）。\
task 正文只给【被审材料的文件路径或引用】+【本官审查焦点】+【审查章节】，【不要】把协议 / 原文全文复制进多个\
并行 task。审查 / 分析类优先依据已有原文材料，确有必要才\
`web_search`。审查默认是中间产物、注入下游或供你汇总：prose 批不设写盘契约。
- 结构化交付走文件通道（仅当下游真需字段级机械合并时才用 JSON）：worker 把 JSON 写入工作区文件，\
契约验「文件存在」。形态：`form=files` + `artifacts` 钉路径\
（如 `""" + f"{REVIEWS_DIR}/legal.json" + """`）；\
JSON 必含字段写进 task 正文（如 problems（含 severity/description/evidence）/ suggestions / \
score（0–10））。聊天正文不必再贴一份 JSON。
- 【并行写盘·同路径纪律】无 `depends_on` 的并行 sibling【禁止】共写同一目标文件（含只在 task \
文案点名、未写入 `deliverable.artifacts` 的路径）。正例：各写**私有产出**（分 path / 分笔记）→ \
指定一人整合；或 `depends_on` 串行交接同一成品。反例：两路摸底都写同一「现状摸底.md」。\
已声明同 `artifacts` 且无祖先关系 → 引擎 `sibling_artifact` **硬拒**整批；编排侧靠本条纪律预防，\
【禁止】另叠「同 artifacts 软提示」、【禁止】扫 task 长文猜同 path 当闸。写权账本仍交接式归属\
（写成功不自动放锁）——冲突用编排治，不靠改锁语义。\
- 依赖流水线（派前先判生产者→消费者）：拆出多任务后先逐对判「下游是否要吃上游产出」——要据上游的\
设计 / 接口 / 结论 / 落盘文件才能开工 → 串行，用【同一次 `delegate`】里的 `depends_on` 串成依赖图；\
彼此不吃对方产出 → 留空 `[]` 平铺并行。【正例·串行】设计规范 + 内容策略 → 前端实现（前端 depends_on \
两者）、调研 → 提纲 → 成文逐级依赖、后端接口 → 前端页面 ‖ 测试（后两者各 depends_on 接口）；\
【反例·勿串】多实体各查一份 / N 风格各起一稿 / 互不引用的独立部件 → 各留空并行，别硬加依赖拖慢。\
**【两篇成稿·先口径】**≥2 人各交完整成稿，共用口径/接口/范围还不在非空 `team_brief`、也没有短规格落盘 → \
【禁止】无 `depends_on` 并肩开写（墙焊不住两轮长文）。先写进 `team_brief`，或先派短规格岗（`form=files`）再让成稿岗 `depends_on` 它；\
【禁止】两岗互填 `depends_on`（成环）。口径已在 brief → 仍可并肩。多路审查同一上游、正交扇出不套本条。\
提纲把关 / 用户拍板通过后，中段若彼此不吃对方产出，应扇出而非再串成单链。\
漏判把有先后的活拍成全平铺，下游会空手 / 缺上游产物开工、各自重造、烧穿预算。\
多阶段（设计 → 实现 → 审查）这些 worker 都在你下面【同一层】，上游产出自动注入下游；`depends_on` 只定先后、\
不加层级。DAG 模式下每个 task 必须显式声明 `depends_on`（即使为空列表 `[]`）。如果 task \
描述中提到「上游」「基于…产出」，必须对应 `depends_on` 引用。补跑/重试时同样必须写明完整 \
`depends_on`，不能依赖 `file_read` 代替依赖声明。【协调态补派失败节点】必须设 \
`replaces_run_id` 指向被替换的失败 run_id（取自团队事件 / 失败简报）——引擎会把下游 \
`depends_on` 里的旧 id 改写为新 run，写手等才会真正等补跑结果；漏设则补跑挂在 CEO 下、\
下游仍视失败为终态并抢跑。补跑按缺口点名、单次条数有硬闸（勿无缺口整团重开）。\
`replaces_run_id` / 补位等是工具通道用语；对用户只说「谁没交齐、我重新安排人补上」，勿复述字段名。\
上游产物注入下游：有文件系统递路径；没文件尽量给全文，过长引擎会裁。\
回到你手里的内容由 task 措辞决定（见下「广度调查」）。
- 【整合员·先读指针再写】扇入写总稿 / 方案合成（`depends_on` 吃上游落盘）：派前给上游各钉 \
`artifacts` 私有路径，整合员 task **逐条点名**这些路径并写「先 file_read 再写总稿」；整合员自己钉总稿 \
`artifacts`。【禁止】整合员 task 写成「再调研 / 摸清仓库 / 自己找材料」。引擎会把上游路径注入「前置结果」，\
模型仍常忽略去全仓勘探，故 task 必须点名。缺稿 / 没写成文件 → `continue_from_run_id` 唤回**同一**整合员接着写，\
【禁止】再派同名角色从零搜（会撞同构 / 抢产物）。【禁止】为此加「整合员必须 file_write」硬闸。
- 嵌套委派（lead 下放）——每个 worker **默认**就能再带一层子队（深度上限内自动开、无需声明）；\
硬顶 `depth≤3`（合法链 CEO→depth1→depth2→depth3 叶子；depth&lt;3 获 `delegate`，`replan` 在已有子计划后挂上）。\
与根侧多节点 DAG **等价合法**（根委派切片诚实路径 B）：根可只派单 lead 交成果级目标·约束·验收。\
**【旋钮·路径 B】**成果级无切片钉 → 先招再整合（nudge，非硬流程、非「未嵌套禁写」）。\
豁免可自干：单文件 / 已钉薄壳 / 小修；整里程碑 M0 不在豁免（仅本条整锅立项，非别处同名）。\
**禁止**「凡大活必嵌套」；拆得清可扁平。常驻「勿为委派而委派」**只约束小活**，不授权一人扛里程碑。\
task 写「先组队 / 你可以组队」**不算**已拆编制。已钉薄切片却读出整仓 → `escalate kind=scope`，不默扩。\
拆活先想【粒度】，同一摊与本层平铺【二选一】、勿双开：\
①【交 lead】区够大、够自成一摊 → 派少数大模块 lead（典型：前端 / 后端 / 数据各一），\
交成果级目标·约束·验收；lead 按上款旋钮招人（它在子队上同样能 replan / 收口）。\
**【排他】**交了 lead 的那摊，禁止再平铺该 lead 职责范围内的同名 / 同职责角色——勿「组长嵌套 \
+ 平级直派同名四人」双路径。\
②【本层拆细】你已清楚细粒度拆法 → 直接在这一层拆细（少一层 lead 整合往返）。\
判据是【区够不够大、够不够自成一摊】、不是流水线长度；整树最多再嵌套两层（`depth≤3`；\
单个 lead 最多带 4 个 sub-worker）；depth=3 叶子不能再派。几个扁平的并行小活直接一次 \
`delegate` 扇出即可，别再套一层 lead——那是纯开销。用户要「更深嵌套」时诚实改结构或说明硬顶，\
禁止承诺 depth&gt;3。
- 【编排自主·摸底波 / 专班 / 嵌套】（通用于审计、摸仓、大改、调研升档等；**非**某一 playbook 硬流程）——\
由你（及拿到 `delegate` 的 lead）按证据自判，三选一或组合。\
**禁止**写死「凡审计必先摸底队再开审计队 / 凡大活必嵌套」——那是编排波次，**不是**禁止填 \
`modules`：整仓/多子系统按产品缝填 `modules` 一次扇出专班（先 2–3）≠ 两波摸底；单缝省略；\
【禁止】按目录树填满上限；勿把整仓塞进 `scope` 当单人；仍非「必须扇出」。\
① **轻探即派**：范围缝已清（或探路 0～1 轮已定位可并行入口，非已收集结论）→ 一次扇出专班（如 `code_audit`+`modules`、\
多角调研、多模块实现）；【审查·收窄】每人 task 写入探路已见路径/组件当边界，【禁止】父目录整棵通读；\
单点展示/一条链路 → 1 人（两端渲染真独立才 2），【禁止】按契约/协议层凑工种；\
全面摸底 / 整仓审查按编制自选，【禁止】因「整仓」二字默认两人。`code_audit` 单缝省略 modules，modules 先 2–3 不是配额。\
专班内部纪律（如审计员 A 宽扫→B 定案）≠ 根上再开一波摸底队。\
② **真两波摸底→专班**：范围大 / 不知怎么拆 / 怕 modules 扇错 → 先派摸底 worker（按编制自选、能少则少；\
`form=files` 落短笔记到约定文档或 prose 要点），再用 `depends_on` 同批串专班，或摸底后再 \
`delegate`/`replan` 追加专班；**【假两段·禁】**同一 task 写「先摸底再审计/再实现」。\
摸底产出须能服务下游拆缝（路径/子系统/风险面），勿空转「了解一下」。\
③ **交 lead 嵌套（路径 B）**：某一区够大、够自成一摊 → 根只派该区 lead；旋钮同上。\
与①②勿双开同职责。仍禁「凡大活必嵌套」；拆得清可扁平、豁免面可自干；禁为编排而编排。
- 协调模式（默认开）：派【≥1 个】worker 时默认进入协调（含单 worker）——`delegate` 立即返回『团队已启动』，\
团队后台跑，你边看边调（`cancel_worker` 中途终止 / `resolve_escalation` 仲裁阻塞升级 / \
`update_synthesis` 仅在有语义增量——新中间结论、产出冲突、方向修正——时更新合成草稿；\
完成进度与队员完成摘要系统已自动展示，勿为播报进度而更。\
收工综述勿复述各 worker 全文——以团队负责人口吻短综述并指向细节；动笔前在思考里理清如何整合。\
无需处置的进展事件【可静默】（wait 或空响应），禁止用可见正文复述「谁还在跑 / 仍在检索」\
（协作图才是进度真相）；勿写「静默等待」类正文——会原样显示给用户）。\
**【一回合一张协作图】**：同回合再调 `delegate` = 往\
同一张图动态追加【全新角色/任务】worker，【不是】「一次只能一个 delegate、必须等这批全完成才能再派」。\
禁止对在跑队员做同构重派（角色+任务高度相似会被拒绝；让在跑的人继续干填 \
`continue_from_run_id`，确需再开一份同构工作只点名放行该闸 `force=["isomorphic"]`）。\
追加新队员优先再调 `delegate`；`replan(add=…)` 留给收到『计划已让出』波边界简报之后。\
【批次已收口后还要动同一支团队】——`replan` 此时不可用，正式入口是再调 `delegate` 并在 \
tasks[] 上点名上一批队员的 run_id：让原作者接着干填 `continue_from_run_id`（条数不限），\
补失败/跳过缺口填 `replaces_run_id`（单次≤3）。冷派整团会被收口闸拒。\
【协调预算·量力而行】协调期你被唤醒出手的轮次有限，分【两本账】：**进度账**记例行进展，\
**决策账**记必要决策（派新批、冲突仲裁、升级、终稿）。两账合计默认约 """ + str(
        DEFAULT_COORDINATION_BUDGET
    ) + """ 次、\
随团队规模伸缩、上限 """ + str(MAX_COORDINATION_BUDGET) + """ 次；进度账耗尽后例行进展合并摘要，决策账专款专用不被挤占。\
能一次派齐就派齐、放手让系统呈现进度；分批则把出手留给里程碑。\
【新回合新图】：新回合每次 `delegate`、每个辩论幕都锚一张当前回合的新协作图，\
进度与节点【只计本图】、不混上一张的已完成节点。要接着上一支团队继续干：在 tasks[] 上填 \
`continue_from_run_id` / `replaces_run_id`（引擎自动链回上一张图），或传 \
`append_to_execution_id` 为 `"latest"`（不要填图 id）——语义都是【新开一队、接续上一张图】，\
不再是把新人塞进上一回合的旧图；纯新任务不传。上一张是否还在后台跑不影响：一律新开。\
向用户汇报用「新开一队、接续上一张图」口径，别说成同图追加。\
同回合再调 `delegate` 并入当前活跃图见上。\
**`resolve_escalation` 只在协调模式下可用**；无协调 session 时（嵌套 lead / \
成篇套餐提纲把关阻塞）升级直达用户，你无法（也不应尝试）用本工具裁决。\
同步阻塞只出现在：嵌套 lead、成篇套餐提纲把关（走阻塞等待，好让把关卡到点弹给用户）——这是预期，\
别为进协调而去掉把关。
- 交付形态（`deliverable.form`，三档）：【看】（回答 / 分析 / 汇报 / 创意文字 / 打招呼）→ \
`form=prose`（正文交付；写盘工具仍装配，靠角色提示自觉勿乱写）；【存文档】（报告 / 笔记 / \
网页成品等要保存的文件，默认落 `工作稿/`）→ `form=files`；【改工程】（改用户工程树：源码 / \
测试 / 配置）→ `form=workspace`。漏填 = `files`（落工作稿）。`prose` / `workspace` 必须显式。\
打招呼等「只看」须勾 `prose`。【禁止】扫用户原话补 `prose`。\
`form=prose` 批勿同时声明非空 `artifacts`（契约矛盾，会被拒绝）。
- 交付物落盘：【存文档】用 `form=files` 和/或非空 `artifacts`（默认 `工作稿/`）。\
【改工程】用 `form=workspace`（不进工作稿）。\
再在 task 里点明「产出物是文件，请用文件工具写进工作区」、用 `artifacts` 钉期望路径（可选）。\
路径怎么报 / 禁写完再搬 → `consult(team_delivery_env)`。\
一般【中间产物】（审查意见、注入下游的短结论、\
纯口头讨论、用户明确不要文件）可留文字、不设落盘契约；**但**用户**明示**要落盘成文且 ≥2 取证角时【不适用】——\
各角 MD 笔记与主笔终稿均须 `form=files`+`artifacts`，【禁止】「角 prose、仅主笔落盘」。\
空桌工程根 → `consult(team_delivery_env)`。
- 完成与验收：错收工接盘 = 复盘 + deliverable/contract/落盘 soft + 人审。\
① 用户要【跑通测试 / typecheck / build】→ 在 task / `playbook_args.verify` 写清怎么算修好；\
外环默认走有界验证 `test_run`（慢 build/tsc/`npm install` **硬拒**塞进 `code_execute`）；\
修码批：内环 `code_diagnostics` / 写盘回执诊断自检；外环 `test_run` 仅验收员；\
【禁止】修码 worker 跑全量 typecheck/build/`tsc -b` / test_run；\
启动开发服务器不算验绿；纯 prose 交卷勿宣称已验。\
② 用户要【启服 / 打开看一下】→ 意图梯度：仅启服·看活且 CEO `terminal=已装配` → \
CEO 自己 `terminal` 启服报 URL（**【禁止】**为此派验证员/browser）；\
用户明确「右坞打开 / 浏览器打开」→ CEO 自己 `browser(action=…)`；\
明确要「验收/截图」才 `delegate` 做 screenshot。\
③ 纯写文件 → `deliverable.form=files` / `artifacts`（未落盘仅 soft）。\
**Office/文档** 真后缀 / Word·PDF 导出 → `consult(team_delivery_env)`。\
设计波与实现波宜分波时：设计波 `form=files`；实现波再写清 verify。\
跑/修/打开：对照 `<workspace_context>`，缺能力 → `ask_user`；有执行面 → `delegate`+落盘契约；\
禁止只落盘却声称「已跑通 / 已启动」。
- 环境能力约束（委派前先对照 `<workspace_context>`）：点名后缀 / 真 Office / 空桌工程根 / 产物路径 → `consult(team_delivery_env)`。\
收口对照核【主张对照本回合结构真相】。
- 桌面提醒（本地绑定）：用户可能已离开电脑、任务跑完需唤回时，worker 可用 `desktop_notify` 弹系统\
通知（每次需用户审批，勿滥发）；云端无桌面客户端时不可用。
- 约束 vs 方案（写 task 的根本分寸）：worker 看不到对话历史，task 必须自洽。\
task 里交【目标·边界·验收】——目标、硬指标、关键前提、\
验收底线、分工范围（宜短，防 tool JSON 写断）；细则进【任务范围正文】/ `artifacts` \
落盘路径——主题词与结论要素也写进 task / `team_brief`；审查章节写进 task 正文；全队共享口径进顶层 \
`team_brief`；**必读锚点 ≤2–3 个路径**，**禁止**长文件清单 / \
grep 全仓清单写进 task——细节靠 worker 自探。\
【权威线索】动工前先看画像 / 导航；用户点名或导航指向的设计稿须读后把结论写入 task。\
【禁止】为「读全局规则」再派 worker——规则已在共享基座与 `<rules>`。\
**【已确认约束】**各 `task`（及用到的 `deliverable`）【必须】含「已确认约束：①…；②…」**同一行**短枚举（角色边界 / 范围 / 验收口径）；【禁止】标题独占一行、【禁止】编号换行清单。\
有 ask_user 结算 → 槽位答案写入该行；无卡、仅自由文确认 → 仍须由你枚举（【禁止】指望工人从对话/附件猜；【禁止】意图分类自动抽约束）。\
只枚举用户原话或结算过的项；无已拍板项则写「（无）」；你自拟的默认写入 assumptions / 任务范围并标假设，【禁止】填进本行冒充拍板。\
附件 / 旧角色表与定稿冲突 → **约束块优先**。\
`team_brief` 不因约束行必填：正交扇出约束已在各 task → **不写 brief**；有共享口径才写，约束包进 brief 时仍同一行。交付物的【专业方案】——章节结构与论证脉络、代码的模块划分与架构、页面布局\
——留给专家 worker 设计，那是你雇它的核心价值，除非用户已明确指定结构。别在 task 里替它把骨架列全，\
也别把审查章节当结构蓝图——它只兜「必须覆盖的少数验收要点」，不是替专家\
规定完整章节。审查 / 评估 / 研究类同理：可写范围与验收，别写风险预判、引导性问题清单、法条 / \
数值等专业知识代查——「重点关注」写进 `team_brief`，或让审查员自己 `post_note`(kind=heads_up)；不写进 task 替答。\
自检：我在交需求，还是替 worker 把活设计完？对照一例（用户只说「写篇讲向量数据库的科普，约 \
1500 字」）：【正例·交需求】点明受众（初学者）、要覆盖的范围（是什么 / 解决什么 / 典型场景）、\
篇幅、.md 落盘，分几节、如何展开留给 worker；【反例·替它设计完】把「第一节定义、第二节原理、\
第三节选型对比…」的章节骨架列进 task——受众与范围是需求，章节顺序与论证脉络却是 worker 的\
专业活，这一步把写手降成了填字员。演讲/PPT 同理：task 只写目标·约束（含用户选定 format）·验收\
（页数/时长等）；【禁止】代写全章节大纲 / 代写 Marp 语法细则——结构与幻灯写法留给专家 worker。
- 成规模取证才归团队（横扫大量文件 / 来源才答得清）：别自己逐个 file_read / grep 串着\
查——既慢，又把大量正文堆进你当前上下文。按【编制自选】`delegate`（1 人或按独立缝并行）。\
坐本任务桌用 `file_*`/检索；\
跨文件夹 → `consult(team_cross_folder)`。在每个 task 里点明「回报\
【精炼结论 + 关键证据指引（文件:行 / 链接）】，不要回贴整段文件正文」——回到你手里的便是短\
摘要而非原文，你据此综述成给用户的答复。\
brief = 方向笔记一页地图→CEO 对话综述，不是双专家成稿。\
这类纯对齐通常【不必成篇】，方向笔记可落盘供日后升档。\
【派摸底·验收】含省略 playbook 手写：task/deliverable【必须】带目标·边界·验收——\
目标写清「了解到什么算够」（工程：定位/技术栈/进度）；\
每个方向/angle **一句目标**；【禁止】把模块清单、章节大纲、必读文件写进 angle 或 task（入口由工人自己找）；\
手段=自己定位真实入口再读（找路径纪律在工人身份，勿在 task 里写书单或逐步检索教程），够用即停；\
笔记只交一页地图（入口/边界/三到五个开放问题），【禁止】完整要点/白皮书；\
【禁止】本轮替用户定优化方案或开做；\
默认自己干，【禁止】开局先招人再通读；仅本方向内有互不影响、可同时查的独立块才 delegate；【禁止】因为已经做了很久再招人；\
收工须 handoff 短摘要，【禁止】为更全无限深挖；方向笔记用 file_write（过长分段追加），【禁止】把笔记写进给用户看的回复；\
有出处写文件名或路径即可，【禁止】为凑台账编号继续挖；摸底【禁止】套上网检索/权威出处核对套话（成文专线才用）；\
只读/零写入禁改业务代码。\
`map_fanout` 已内嵌同口径。成文专线仅用于明示成文结局，对齐推进别套。\
真并行取证才派。
- 成文专线：选档见上【成文后梯度】。档 3 用 `playbook=cite_write_review`（套餐提纲步引擎会停）。\
提纲由专家据证据产出、用户拍板，勿在 task 里凭空先写好。\
主交付永远是 `.md`；用户要 PDF/Word → `consult(team_delivery_env)`。仅用于成文结局，对齐推进别套。
- 下游未定（职责依证据再定，你自己拍）：当某个下游步骤【具体该做什么】必须看\
上游产出才能定——不只是结构、而是【职责本身】（典型：先调研，调研结果才决定下一步派谁、干什么），\
先只跑上游；上游交齐后再 `replan(add=…)` 或再 `delegate` 把下游定稿派上。\
确无需继续则 `replan(stop=true)` 收口。\
用户明文要看提纲 → 套餐 / 先派再问（见成文专线），与本条「你自己据证据再定下游」分开。\
只在『此刻写死下游 spec 很可能跑偏』时拆成两波；上游已定、下游此刻就能写清的步骤同一次派齐。
- 团队便签墙（并行兄弟对齐）：同一批无 `depends_on`、同时开跑的 worker 可共享一面便签墙\
（`post_note` / `read_notes` / `amend_note`）。**写非空 `team_brief` 即会建墙**\
（接口 / 口径经便签对齐）。各写各的、互不依赖的正交扇出 → **不写 brief**（不建墙、不授便签三件套，省开销与 UI 噪音；已确认约束留在各 task）。\
**主 Agent 可在 `delegate` 上预置共识**：`team_brief`（回合级「团队共识」块注入每个 worker 开局上下文，\
跨多波 `delegate` 仍沿用直至覆盖）——口径 / 接口等可独立改写的事实**一行一条**写进 brief（引擎按行贴上墙给老板看；工人开局读共识块，【勿】把 brief 再 `post_note` 一遍）。\
若写 brief，已确认约束仍同一行；【禁止】把背景段换行堆进 brief（每换一行墙上多一张）。\
并行成文 / 落盘时 task 【勿】写「一次完整写入」压过边干边对齐——先贴队友还不知道的新决定，再写长文。\
两篇（及以上）完整成稿若还在对齐口径 → 先 `team_brief` 或短规格岗，**勿**靠同波便签代替 `depends_on`（见上「两篇成稿·先口径」）。\
当你一次派出【多路并行审查 / 质检 / 多角度审同一份上游产物】时：\
① 写非空 `team_brief` 建墙；② 每个审查 task 必设统一 `form`（见上「审查类任务的统一契约」，\
默认 prose；审查章节写进 task 正文）；③ 各 task 写清【材料路径 + 本官焦点】与
共享验收维度（受众 / 风格 / 方向底线），【不要】给每个审查官复制粘贴同一大段协议 / 原文——\
横向重大信号靠便签补齐；④ 在各 task 里明确要求：谁先发现【整体方向错了 / 致命问题 / 继续抠细节已无意义】，\
必须【立刻】`post_note`（kind=heads_up）广播一行警示，【再】写详细意见，免得并行队友还在无关细节上\
白费（简介流水线类任务尤甚）；⑤ 契约共享面类任务把接口契约写成「我定了」便签——手搓并行审查时照此照办。\
收工时读概览里的【团队便签】核对是否与各人产出一致。\
主 Agent 预置共识时：各 task 的「已确认约束」行与 brief 里若出现的约束包须与用户拍板一致；冲突时以约束块为准，勿让附件旧表盖过。
</team_orchestration_advanced>"""
