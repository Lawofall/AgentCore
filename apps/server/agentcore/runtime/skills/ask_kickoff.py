"""Skill body: ask_kickoff (开场提问)."""

from __future__ import annotations

_ASK_KICKOFF = """\
<开场提问>
字段 HOW：拿不准 `ask_user` 字段时查阅本条。

【短改稿】用户给了改稿点 → 先复述再答或派。缺信息就向用户发问。

【决策】有倾向时填 `default`。派工跟勾选/人话走。空 continue 用该 default 并标「按确认默认」。\
「继续」≠ 上轮选项已确认：须复述那些选项（或卡上 default）。\
未点名主体 ≠ 自拟后派。无勾选且用户未提交不得把自拟主体当已确认。\
方向 / 方案 choice 的 `label` 写清本轮交付边界；权衡写进选项名；\
选完仍立刻派，范围跟选项走 ≠ 暗示「选完即全仓开工」。

【交付】choice 的 `label` 只写用户要拿到的结果 ≠ 怎么拆团队。\
形态未钉先问；已钉立刻派那个结果。建站用手写 `tasks`。

现有能力做不到 → `message` 第一句说清做不到什么，再给替代；用户坚持 → 按用户所选继续。\
明显次优 → 标假设继续。说满 ≠ 空派。本钩只管载体·手段 ≠ 把形态提问扩成载体审讯。

【字段】问句写 `prompt`（要什么 / 给谁 / 做到哪一档）。\
有倾向时该项放第一、名末加「（推荐）」；卡面不预选。\
choice 只服务下一步动作 ≠ 把正文已摆出的候选菜单再投进卡里催收敛。\
专用 `card`：仅 `organize_plan`（恰好 1 题多选）。
</开场提问>"""
