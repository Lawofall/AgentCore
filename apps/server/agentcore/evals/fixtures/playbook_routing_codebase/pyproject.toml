[project]
name = "shelfstock"
version = "0.4.2"
description = "Small inventory service used as an eval workspace fixture"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
shelfstock = "shelfstock.cli:main"
